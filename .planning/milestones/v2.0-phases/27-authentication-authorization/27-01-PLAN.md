# Phase 27 Plan: Authentication & Authorization

**Phase goal:** JWT login, token refresh, /auth/me endpoint, get_current_user dependency, require_role factory, bcrypt password hashing.

**Requirements:** AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06

Files created/updated:
- app/database.py — SQLAlchemy session factory with get_db dependency
- app/services/auth.py — hash_password, verify_password, create_access_token, decode_token, authenticate_user
- app/schemas/auth.py — LoginRequest, TokenResponse, UserProfile
- app/schemas/user.py — UserResponse, UserCreate, UserRoleUpdate
- app/dependencies.py — get_current_user (real JWT validation), require_role factory
- app/routers/auth.py — POST /login, POST /refresh/token, GET /me
