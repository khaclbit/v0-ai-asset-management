# Phase 25 Plan: Backend Scaffold & Docker Environment

**Phase goal:** FastAPI backend boots in Docker Compose; project structure is established; Alembic is initialized; seed script is idempotent. All ENV-01–06 requirements delivered.

**Requirements:** ENV-01, ENV-02, ENV-03, ENV-04, ENV-05, ENV-06

---

## Wave 0 — Directory structure

Create `backend/` folder layout:
```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py           ← FastAPI app factory + CORS + router registration
│   ├── config.py         ← pydantic-settings, reads .env
│   ├── dependencies.py   ← stub (get_current_user implemented in Phase 27)
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── auth.py       ← stub
│   │   ├── assets.py     ← stub
│   │   ├── users.py      ← stub
│   │   ├── assignments.py ← stub
│   │   └── maintenance.py ← stub
│   ├── models/
│   │   ├── __init__.py
│   │   └── base.py       ← DeclarativeBase
│   ├── schemas/
│   │   └── __init__.py
│   └── services/
│       └── __init__.py
├── alembic/
│   └── versions/         ← (alembic init creates env.py, script.py.mako)
├── alembic.ini
├── requirements.txt      ← pinned deps
├── .env.example          ← all env vars documented
├── seed.py               ← creates first Admin user, idempotent
└── Dockerfile
```

And at project root:
- `docker-compose.yml`

## Wave 1 — Core files

1. `backend/requirements.txt` — pinned versions
2. `backend/.env.example` — all env vars
3. `backend/Dockerfile` — python:3.11-slim, uvicorn
4. `docker-compose.yml` (project root) — api + db + pgadmin
5. `backend/app/config.py` — pydantic-settings
6. `backend/app/main.py` — FastAPI app, CORS, routers
7. `backend/app/dependencies.py` — stub
8. Router stubs (auth, assets, users, assignments, maintenance)
9. `backend/app/models/base.py` — DeclarativeBase

## Wave 2 — Alembic + seed

1. `backend/alembic.ini` — configured for env var DATABASE_URL
2. `backend/alembic/env.py` — imports models, uses DATABASE_URL from env
3. `backend/alembic/script.py.mako`
4. `backend/alembic/versions/` — empty, ready for Phase 26 migration
5. `backend/seed.py` — idempotent admin user creation

## Verification

- `docker compose up` should show FastAPI and PostgreSQL starting
- `curl http://localhost:8000/docs` returns the OpenAPI UI
- `curl http://localhost:8000/api/v1/health` returns `{"status": "ok"}`
- Seed script can be run twice without error

---

**ENV-01:** backend/ structure ✅
**ENV-02:** docker-compose.yml (api + db + pgadmin) ✅
**ENV-03:** .env.example documented ✅
**ENV-04:** alembic/ initialized ✅
**ENV-05:** requirements.txt pinned ✅
**ENV-06:** seed.py idempotent ✅
