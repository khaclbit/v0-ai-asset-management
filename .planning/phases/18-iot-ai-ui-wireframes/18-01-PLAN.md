---
phase: 18-iot-ai-ui-wireframes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md
autonomous: true
requirements: [UX-05, UX-06, UX-07, UX-08, UX-09]

must_haves:
  truths:
    - "IoT Monitoring wireframe (§6) documents 2-panel layout: left asset-selector sidebar with live status dots (4 states), per-category sensor tile grid (Laptop: 5 tiles, Printer: 6, Forklift: 5, Monitor: 4, OE: 4), time window selector (1h/6h/24h/7d), LineChart+ReferenceLine per active sensor, and connection status Badge"
    - "AI Predictive wireframe (§7) documents recommendation cards with StatusBadge risk chips (High/Medium/Low), confidence score, top 3 factors ul.list-disc, Manager/Admin-only Approve+Defer buttons, SLA countdown Clock3+formatSlaCountdown, overdue escalation banner, and read-only Badge for non-Manager roles — sourced from existing predictive/page.tsx gold standard"
    - "Notification Center wireframe (§8) documents Topbar bell icon modification (Bell + destructive Badge, navigates to /dashboard/notifications full page — NOT a dropdown), and /notifications page with notification rows (type icon/title/message/timeAgo/unread-dot/deep-link), mark-as-read (individual), mark-all-read (CardAction), pagination"
    - "Audit Log wireframe (§9) confirms existing /dashboard/audit implementation: category filter buttons (All/Business/Security/AI-assisted), immutable 8-column table (Actor/Action/Entity/Before/After/Timestamp/Correlation ID/Details), expandable row with colSpan=8 muted detail panel — no edit/delete/sort actions"
    - "User Management wireframe (§10) documents Admin-only /dashboard/users with role-badged user table, /users/new create form with 5 fields, /users/[id]/edit with reactivate toggle, and Deactivate confirmation Dialog (soft-delete only — absolutely no hard-delete button anywhere)"
    - "Route migration TODO documented in WIREFRAMES_2.md header: /dashboard/predictive → /dashboard/ai; create app/dashboard/ai/page.tsx; add Next.js redirect from /dashboard/predictive"
  artifacts:
    - path: ".planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md"
      provides: "Annotated ASCII wireframes for UX-05, UX-06, UX-07, UX-08, UX-09"
      contains: "UX-05-VERIFIED UX-06-VERIFIED UX-07-VERIFIED UX-08-VERIFIED UX-09-VERIFIED"
  key_links:
    - from: "WIREFRAMES_2.md §6 IoT Monitoring"
      to: "18-RESEARCH.md §IoT Monitoring Page Architecture"
      via: "SENSOR_CATEGORY_MAP table, LineChart+ReferenceLine pattern, connection status Badge states"
      pattern: "SENSOR_CATEGORY_MAP"
    - from: "WIREFRAMES_2.md §7 AI Predictive"
      to: "frontend/app/dashboard/predictive/page.tsx"
      via: "recommendation card structure, getHighRiskSlaState, approval gate pattern"
      pattern: "getHighRiskSlaState"
    - from: "WIREFRAMES_2.md §8 Notification Center"
      to: "components/topbar.tsx"
      via: "Bell icon + destructive Badge addition spec"
      pattern: "Bell.*destructive"
    - from: "WIREFRAMES_2.md §9 Audit Log"
      to: "frontend/app/dashboard/audit/page.tsx"
      via: "confirms implemented design; colSpan=8 expandable row pattern"
      pattern: "colSpan.*8"
    - from: "WIREFRAMES_2.md §10 User Management"
      to: "18-RESEARCH.md §User Management Architecture"
      via: "role badge mapping, deactivate dialog, no hard-delete constraint"
      pattern: "is_active.*false"
---

<objective>
Produce WIREFRAMES_2.md — the annotated ASCII wireframe document for AssetIQ v1.2's five
remaining dashboard pages: IoT Monitoring (UX-05), AI Predictive Maintenance (UX-06),
Notification Center (UX-07), Audit Log (UX-08), and User Management (UX-09).

Purpose: These wireframes are the design authority for Phase 19+ implementation. They
capture layout, component mapping, data source annotations, role-permission rules, and
anti-patterns in a single executable reference. The existing predictive/page.tsx and
audit/page.tsx serve as gold-standard references; the wireframes align to them precisely.

Output: `.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md` — a self-contained
design document continuing from Phase 17's WIREFRAMES.md (which covered §1–§5).
WIREFRAMES_2.md covers §6–§10 using identical annotation style.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-iot-ai-ui-wireframes/18-RESEARCH.md
@.planning/phases/17-core-ui-wireframes/WIREFRAMES.md
@frontend/app/dashboard/predictive/page.tsx
@frontend/app/dashboard/audit/page.tsx
@.planning/phases/16-design-system-component-catalog/DESIGN_SYSTEM.md
@.planning/phases/15-information-architecture-user-flows-navigation/IA.md
@.planning/phases/14-system-architecture-domain-model/SDD.md
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════════
     WAVE 0: Scaffold WIREFRAMES_2.md + Route Migration TODO
     ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" id="18-01-T01">
  <name>Task 18-01-T01 (Wave 0): Create WIREFRAMES_2.md — header, metadata, and route migration TODO</name>
  <files>.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</files>
  <action>
Create `.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md` as a new file. Write the
following content exactly (WIREFRAMES_2.md is a continuation of Phase 17's WIREFRAMES.md
which covers §1–§5; this document covers §6–§10):

---
YAML front matter block:
  phase: 18
  document: WIREFRAMES_2
  status: complete
  date: 2026-06-28
  requirements: UX-05, UX-06, UX-07, UX-08, UX-09
  depends_on:
    - Phase 14 SDD.md (sensor category map §2.6, AI state machine §2.4, notification pipeline §1.5, audit §1.6)
    - Phase 15 IA.md (routes, user flows §5 IoT, §6 AI, §7 Notifications)
    - Phase 16 DESIGN_SYSTEM.md (§5.2 LineChart+ReferenceLine, §1 color tokens)
    - Phase 17 WIREFRAMES.md (§1–§5; shell constant, component conventions)
    - frontend/app/dashboard/predictive/page.tsx (AI Predictive gold standard)
    - frontend/app/dashboard/audit/page.tsx (Audit Log confirmed implementation)
---

Then write the document title and scope preamble:

  # AssetIQ — IoT & AI UI Wireframes (WIREFRAMES_2)

  > **Scope:** Annotated ASCII wireframes for five dashboard pages: IoT Monitoring (§6),
  > AI Predictive Maintenance (§7), Notification Center (§8), Audit Log (§9), and
  > User Management (§10). Continues WIREFRAMES.md (Phase 17). All shell constants,
  > component library rules, and annotation conventions from WIREFRAMES.md §1 apply here.
  >
  > **Shell constant:** Sidebar (w-64, bg-sidebar #0E1825) + Topbar (h-16, sticky,
  > border-b) + Page Content (p-6, space-y-6, overflow-y-auto). Wireframes below show
  > only the PAGE CONTENT zone unless stated otherwise.
  >
  > **Component library:** shadcn/ui + Recharts 3.8. No MUI imports. No raw hex colors —
  > always use var(--chart-N) or Tailwind token classes.

Then write a `## ⚠ Route Migration TODO` section (this is Wave 0's sole deliverable):

  ## ⚠ Route Migration TODO

  **Identified Gap:** The sidebar navigation (`lib/navigation-access.ts`) declares the
  canonical AI Predictive route as `/dashboard/ai`, but the current implementation lives
  at `app/dashboard/predictive/page.tsx`. The sidebar nav item "AI Predictive" therefore
  produces a 404.

  **Required Actions (Phase 19 implementation):**

  1. Create `app/dashboard/ai/page.tsx` — move implementation from `predictive/page.tsx`
     (change `<Topbar title="Predictive Maintenance" />` to `<Topbar title="AI Predictive" />`
     and update `subtitle` to match). The component logic is identical.
  2. Create `app/dashboard/predictive/page.tsx` redirect:
     ```
     // app/dashboard/predictive/page.tsx
     import { redirect } from "next/navigation"
     export default function PredictiveLegacyRedirect() {
       redirect("/dashboard/ai")
     }
     ```
  3. Do NOT delete `app/dashboard/predictive/` until redirect is confirmed working in
     production. Keep both files until Phase 20 cleanup.

  **Impact:** UX-06 wireframe in §7 targets `/dashboard/ai` as the canonical route.
  All deep-links in notifications (§8) also use `/dashboard/ai?assetId={id}`.

  ---

The file should end after this section — subsequent tasks will append §6–§10.
  </action>
  <verify>
    <automated>grep -c "Route Migration TODO" .planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</automated>
  </verify>
  <done>WIREFRAMES_2.md exists, front matter is valid, preamble matches WIREFRAMES.md §1 shell constant conventions, and the Route Migration TODO section documents the /dashboard/predictive → /dashboard/ai gap with the redirect snippet and 3-step resolution plan.</done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════
     WAVE 1: IoT Monitoring (UX-05) + AI Predictive (UX-06)
     Sequential due to single-file ownership; IoT first, then AI
     ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" id="18-01-T02">
  <name>Task 18-01-T02 (Wave 1a): §6 IoT Monitoring wireframe — 2-panel layout, sensor tiles, charts (UX-05)</name>
  <files>.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</files>
  <action>
Append `## §6 IoT Monitoring (/dashboard/iot) (UX-05)` to WIREFRAMES_2.md. The section
must contain all sub-sections listed below, using ASCII art and annotation style matching
WIREFRAMES.md (see §3 Asset Management for style reference).

**Route and roles header:**
  Route: `/dashboard/iot` and `/dashboard/iot/[assetId]`
  Roles: Admin, Manager (Staff → /403 per navigation-access.ts)
  Data note: WebSocket WS /api/iot/{assetId}/stream (5s aggregation window, FastAPI MQTT consumer → PostgreSQL)

**Sub-section: Topbar**
Write the topbar as a pre-formatted block:

  TOPBAR:
  ┌────────────────────────────────────────────────────────────┐
  │  "IoT Monitoring"  (h4)   "Live sensor telemetry"  (caption)│
  │                                           [🔔 N]  [👤 ▾]  │
  └────────────────────────────────────────────────────────────┘

**Sub-section: Full 2-Panel Page Layout**
Write the overall page layout showing the two-panel shell. Left panel is the asset
selector sidebar (in-page aside, NOT the global nav sidebar — w-64, border-r). Right
panel is the main content area. Show the full layout as:

  PAGE CONTENT (p-6, flex gap-6):
  ┌──────────────────────────┬──────────────────────────────────────────────────────┐
  │  ASSET SELECTOR          │  CONTENT PANEL                                       │
  │  (aside, w-64, border-r, │                                                      │
  │   overflow-y-auto)       │  HEADER ROW (flex justify-between items-center mb-4)│
  │                          │  ┌──────────────────────────────────────────────┐   │
  │  p className="text-xs    │  │ h2 "Dell Latitude 5540"  text-lg font-semibold│  │
  │  font-semibold uppercase │  │ "SN-ABC1234 · Laptop"  text-sm text-muted    │  │
  │  tracking-wide           │  └──────────────────────────────────────────────┘  │
  │  text-muted-foreground   │  ┌───────────────────────────────────────────┐     │
  │  px-2 pb-2"              │  │ [Badge] ● Live  (bg-chart-3/15 text-chart-3│     │
  │  "ASSETS"                │  │   OR    ◌ Reconnecting... (bg-chart-4/15)  │     │
  │  ─────────────           │  │   OR    ✕ Disconnected  (bg-destructive/15)│     │
  │  ● Dell L7420     [✓]    │  │ Data: WS connection state                  │     │
  │  ✕ Forklift FL-01        │  └───────────────────────────────────────────┘     │
  │  ◌ Canon Printer         │                                                      │
  │  ● HP Monitor            │  TIME WINDOW SELECTOR (flex gap-1 mb-4)             │
  │  ● Ricoh OE              │  [1h] [6h] [24h] [7d]  ← Button group;             │
  │                          │  active variant="default", inactive variant="outline"│
  │  ──────────────────────  │  Controls X-axis domain for all charts below        │
  │  TIME WINDOW             │                                                      │
  │  ┌────────────────────┐  │  SENSOR TILE GRID (see §6.1 Tile Grid below)        │
  │  │ ○ 1h  ● 6h  ○ 24h │  │                                                      │
  │  │ ○ 7d              │  │  SENSOR CHARTS (see §6.2 Charts below)               │
  │  └────────────────────┘  │                                                      │
  └──────────────────────────┴──────────────────────────────────────────────────────┘

**Sub-section: §6.1 Asset Selector Sidebar Detail**
Document the sidebar as a scrollable aside list. Each asset row is a button:

  ASSET ROW (Button, w-full, flex items-center gap-2, px-2 py-1.5, rounded-md):
  ┌─────────────────────────────────────────────────┐
  │  [●]  Dell Latitude 5540          [selected bg] │  ← bg-accent when active
  │       text-sm font-medium                       │
  │       text-xs text-muted-foreground "Laptop"    │
  └─────────────────────────────────────────────────┘
  ┌─────────────────────────────────────────────────┐
  │▌ [✕]  Forklift FL-01                            │  ← border-l-4 border-destructive
  │       "Equipment"                               │    when threshold violation active
  └─────────────────────────────────────────────────┘

Status dot color rules (w-2 h-2 rounded-full span):
  ● bg-chart-3  (green)  = Connected, reading < 30s old
  ✕ bg-destructive (red) = Threshold violation active on this asset
  ◌ bg-chart-4  (amber)  = Stale (no reading > 60s)
  ○ bg-muted-foreground (grey) = Offline / no sensor device linked

Default: first asset pre-selected on page load (IA Flow 5).
Admin/Manager see all assets. Staff role does not have access to this page.

**Sub-section: §6.1 Sensor Tile Grid (category-aware)**
Document the tile grid with the category mapping table. The grid uses:
  className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-4"

Category → sensor tile mapping (VERIFIED: SDD §2.6):

  | Asset Category  | Tiles Shown (count)                                         |
  |-----------------|-------------------------------------------------------------|
  | Laptop          | Temperature, Humidity, Power, Current, Running Hours (5)    |
  | Monitor         | Temperature, Power, Current, Running Hours (4)              |
  | Printer         | Temperature, Humidity, Power, Current, Vibration, Hours (6) |
  | Forklift        | Temperature, Power, Current, Vibration, Running Hours (5)   |
  | Office Equipment| Temperature, Humidity, Power, Running Hours (4)             |

NOTE: Humidity tile is ABSENT for Forklift and Monitor. Vibration tile is ABSENT for
Laptop, Monitor, and Office Equipment. Never render a tile for a sensor not in the
category's active list — the grid must be built from SENSOR_CATEGORY_MAP[asset.category].

Each tile is a compact Card (Variant A — p-3, no CardHeader):

  SENSOR TILE (Card):
  ┌──────────────────────┐   ┌──────────────────────┐   ┌──────────────────────┐
  │ TEMPERATURE          │   │ HUMIDITY             │   │ POWER                │
  │ text-xs uppercase    │   │ text-xs uppercase    │   │ text-xs uppercase    │
  │ tracking-wide        │   │ tracking-wide        │   │ tracking-wide        │
  │ text-muted-fgrd      │   │ text-muted-fgrd      │   │ text-muted-fgrd      │
  │                      │   │                      │   │                      │
  │  72°C                │   │  68%                 │   │  450W                │
  │  text-2xl font-bold  │   │  text-2xl font-bold  │   │  text-2xl font-bold  │
  │  font-mono           │   │  font-mono           │   │  font-mono           │
  │  text-destructive    │   │  text-foreground     │   │  text-foreground     │
  │                      │   │                      │   │                      │
  │  ⚠ Above limit       │   │  Normal              │   │  Normal              │
  │  text-xs             │   │  text-xs             │   │  text-xs             │
  │                      │   │                      │   │                      │
  │  Updated 5s ago      │   │  Updated 5s ago      │   │  Updated 5s ago      │
  │  text-xs text-muted  │   │  text-xs text-muted  │   │  text-xs text-muted  │
  └──────────────────────┘   └──────────────────────┘   └──────────────────────┘
  ↑ border-destructive        ↑ no accent border           ↑ no accent border
  (violation: value > threshold)
  ↑ text-chart-4 (near: value > threshold * 0.8)

Threshold color logic:
  Normal (value ≤ threshold):         text-foreground, no border accent
  Near-threshold (> 80% of limit):    text-chart-4 (amber), no border accent
  Violation (value > threshold):      text-destructive, border-destructive on Card

Unit display: from MQTT payload `unit` field (°C, %, W, A, mm/s, hours).
Data source annotation: WS /api/iot/{assetId}/stream

**Sub-section: §6.2 Time-Series Line Charts**
Document the chart stack below the tile grid. One chart per active sensor type, stacked
vertically (space-y-6). Each chart:

  CHART: Temperature over time
  ┌────────────────────────────────────────────────────────────────────────────┐
  │  ChartContainer className="h-[280px]"                                      │
  │  ┌──────────────────────────────────────────────────────────────────────┐  │
  │  │ 90 ─│                                                                │  │
  │  │ 80 ─│                    /\           /\                             │  │
  │  │ 70 ─│- - - - - - - - - -/--\- - - - -/--\- - - - -  ← ReferenceLine│  │
  │  │ 60 ─│               /\/      \   /\/      \/\                        │  │
  │  │ 50 ─│           /\/                            \                     │  │
  │  │ 40 ─│       /\/                                 \                    │  │
  │  │     └─────────────────────────────────────────────────────           │  │
  │  │      10:00  10:30  11:00  11:30  12:00  12:30         ← X-axis      │  │
  │  │  ─── Temperature (°C)     ─ ─ Threshold (70°C)        ← Legend      │  │
  │  └──────────────────────────────────────────────────────────────────────┘  │
  └────────────────────────────────────────────────────────────────────────────┘

Components used (VERIFIED: DESIGN_SYSTEM §5.2):
  - <ChartContainer className="h-[280px]">
  - <LineChart data={readings}>
  - <Line dataKey="value" stroke="var(--chart-1)" dot={false} strokeWidth={2} />
  - <ReferenceLine y={threshold} stroke="var(--destructive)" strokeDasharray="4 4" strokeWidth={1.5} />
  - <XAxis dataKey="ts" tickFormatter={formatXAxis} />
  - <YAxis tickFormatter={(v) => `${v}${unit}`} width={48} />
  - <ChartTooltip content={<ChartTooltipContent />} />
  - <ChartLegend content={<ChartLegendContent />} />
  Empty state: <ChartEmptyState message="No sensor readings in this time window" />

Chart color → sensor mapping (DESIGN_SYSTEM §1.6):
  chart-1 #0864CD → Temperature
  chart-2 #00B6B7 → Humidity
  chart-3 #43A74C → Power Consumption
  chart-4 #ED980E → Current
  chart-5 #E23431 → Vibration
  chart-6 #8B5CF6 → Running Hours

X-axis timestamp formatting:
  1h/6h  → toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' })
  24h/7d → format(new Date(ts), 'MM/dd HH:mm')  (date-fns)

Data source: GET /api/iot/{assetId}/readings?window={1h|6h|24h|7d} (REST, initial load)
             WS /api/iot/{assetId}/stream (live append — 5s batches push new data points)

Threshold data source: GET /api/iot/thresholds (per-category threshold config)

End the §6 section with a role-action table:
  | Action                     | Admin | Manager | Staff        |
  |----------------------------|:-----:|:-------:|:-------------|
  | View IoT page              | ✅    | ✅      | 🔒 /403      |
  | See all asset sensors      | ✅    | ✅      | — (blocked)  |
  | Time window selection      | ✅    | ✅      | — (blocked)  |

Then append the UX-05 verification marker: <!-- UX-05-VERIFIED -->
  </action>
  <verify>
    <automated>grep -c "UX-05-VERIFIED" .planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</automated>
  </verify>
  <done>WIREFRAMES_2.md contains §6 IoT Monitoring with: (1) 2-panel ASCII layout showing asset selector aside + content panel, (2) connection status Badge with 3 states, (3) sensor tile grid with category table showing correct counts (Laptop 5, Printer 6, Forklift 5, Monitor 4), (4) tile component anatomy with threshold color rules, (5) chart stack spec with LineChart+ReferenceLine components, (6) chart color → sensor mapping, (7) x-axis timestamp format rules, (8) role-action table, (9) UX-05-VERIFIED marker.</done>
</task>

<task type="auto" id="18-01-T03">
  <name>Task 18-01-T03 (Wave 1b): §7 AI Predictive Maintenance wireframe — recommendation cards, SLA, approval gate (UX-06)</name>
  <files>.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</files>
  <action>
Append `## §7 AI Predictive Maintenance (/dashboard/ai) (UX-06)` to WIREFRAMES_2.md.
This section derives directly from `frontend/app/dashboard/predictive/page.tsx` (the gold
standard implementation) — read that file before writing this section. The canonical route
is /dashboard/ai (NOT /dashboard/predictive — see §⚠ Route Migration TODO above).

**Route and roles header:**
  Route: `/dashboard/ai`  (canonical per navigation-access.ts; old /dashboard/predictive → redirect)
  Roles: Admin, Manager (Staff → /403)
  Note: Approve/Defer buttons are role-gated UI convenience — RBAC enforced server-side at
        POST /api/ai/recommendations/{id}/approve (per SDD §2.4)

**Sub-section: Topbar**

  TOPBAR:
  ┌────────────────────────────────────────────────────────────────────────────────────┐
  │  "AI Predictive"  (h4)   "Deterministic risk-ranked recommendations"  (caption)   │
  │                                                                [🔔 N]  [👤 ▾]     │
  └────────────────────────────────────────────────────────────────────────────────────┘

**Sub-section: Predictive Summary Card**
This card is always at the top of the page, full width (from existing implementation):

  PREDICTIVE SUMMARY CARD (Card, full-width):
  ┌───────────────────────────────────────────────────────────────────────────┐
  │ CardHeader:                                                               │
  │  [TrendingUp icon]  "Predictive Summary"  (CardTitle text-base)          │
  │  "Recommendations sorted by risk first, then confidence. High-risk items  │
  │   include SLA monitoring and escalation state."  (CardDescription)       │
  ├───────────────────────────────────────────────────────────────────────────┤
  │ CardContent: grid gap-3 sm:grid-cols-3                                    │
  │ ┌───────────────────────────────┐ ┌───────────────────────────┐ ┌──────┐ │
  │ │ "Total recommendations"       │ │ "Pending high-risk"       │ │"Act" │ │
  │ │  text-xs text-muted           │ │  text-xs text-muted       │ │ pol" │ │
  │ │  12                           │ │  3                        │ │ "You │ │
  │ │  text-lg font-semibold        │ │  text-lg font-semibold    │ │ can  │ │
  │ │  rounded-lg border bg-muted/30│ │  rounded-lg border        │ │ appr │ │
  │ │  p-3                          │ │  bg-muted/30 p-3          │ │ or   │ │
  │ └───────────────────────────────┘ └───────────────────────────┘ │defer"│ │
  │   Data: recommendations.length     pending high-risk count       └──────┘ │
  │                                                           ↑ "Read-only for │
  │                                                             role: {role}"  │
  │                                                             if not Manager │
  └───────────────────────────────────────────────────────────────────────────┘

**Sub-section: Recommendations Grid**
Section header: h2 className="text-sm font-semibold uppercase tracking-wide text-muted-foreground"
"RECOMMENDATIONS"
Grid: div className="grid gap-4 xl:grid-cols-2"

Document two card variants side by side — High-risk card and Medium/Low-risk card:

  HIGH-RISK RECOMMENDATION CARD                    MEDIUM/LOW-RISK CARD
  (Card border-destructive/30)                     (Card — default border)
  ┌─────────────────────────────────────────┐      ┌──────────────────────────────────┐
  │ CardHeader space-y-2:                   │      │ CardHeader space-y-2:            │
  │  flex justify-between items-center gap-2│      │  flex justify-between gap-2:     │
  │  "Dell Latitude 5540"  (CardTitle base) │      │  "HP LaserJet Pro M404"          │
  │  ┌──────────┐ ┌───────────────────────┐ │      │  ┌───────────┐ ┌──────────────┐ │
  │  │ [High]   │ │Risk score: 94%        │ │      │  │ [Medium]  │ │Risk: 58%     │ │
  │  │StatusBadge│ │Badge variant="outline"│ │      │  │StatusBadge│ │Badge outline │ │
  │  └──────────┘ └───────────────────────┘ │      │  └───────────┘ └──────────────┘ │
  │  "Confidence: 87% (High)"  CardDesc     │      │  "Confidence: 62% (Medium)"      │
  │  formatConfidenceScore(score) + band    │      │  formatConfidenceScore(score)     │
  ├─────────────────────────────────────────┤      ├──────────────────────────────────┤
  │ CardContent space-y-4 text-sm:          │      │ CardContent space-y-4 text-sm:   │
  │                                         │      │                                  │
  │  Correlation ID row:                    │      │  Correlation ID row (same):      │
  │  ┌─────────────────────────────────┐    │      │  ┌──────────────────────────┐    │
  │  │ "AI Correlation ID:"            │    │      │  │ "AI Correlation ID:"     │    │
  │  │ font-medium                     │    │      │  │ CORR-2024-0987           │    │
  │  │ CORR-2024-0042                  │    │      │  │ font-mono text-xs        │    │
  │  │ font-mono text-xs               │    │      │  └──────────────────────────┘    │
  │  │ rounded-lg border bg-muted/30 p-3│   │      │                                  │
  │  └─────────────────────────────────┘    │      │  Top Factors:                    │
  │                                         │      │  "TOP FACTORS" label             │
  │  Top Factors:                           │      │  ul.list-disc.pl-5               │
  │  p "TOP FACTORS" text-xs uppercase      │      │  · High print volume (30d)       │
  │  ul className="list-disc space-y-1 pl-5 │      │  · Drum wear indicator 68%       │
  │     text-muted-foreground":             │      │  · Toner low warnings ×3         │
  │  · High temperature variance (72°C avg) │      │                                  │
  │  · Running hours 2,840 (threshold 2k)   │      │  [No SLA section — not High]     │
  │  · Humidity sensitivity alert           │      │                                  │
  │                                         │      │  Separator                       │
  │  HIGH-RISK SLA + APPROVAL SECTION:      │      │  Trace & provenance:             │
  │  div rounded-lg border bg-muted/20 p-3  │      │  <AiTracePanel> (collapsed)      │
  │  ┌─────────────────────────────────┐    │      └──────────────────────────────────┘
  │  │ [Clock3 icon]  SLA: 1h 43m      │    │
  │  │ font-medium  ← formatSlaCountdown│   │
  │  │ [Badge destructive "Overdue"]    │    │
  │  │ ← shown only when isOverdue=true │    │
  │  ├─────────────────────────────────┤    │
  │  │ ESCALATION BANNER (conditional): │    │
  │  │ div border border-destructive/40 │    │
  │  │     bg-destructive/10 text-destr │    │
  │  │ [AlertTriangle icon]             │    │
  │  │ "Escalation required: SLA overdue│    │
  │  │  by {formatSlaCountdown(over.)}."│    │
  │  ├─────────────────────────────────┤    │
  │  │ APPROVAL STATE (conditional):    │    │
  │  │ • approved:                      │    │
  │  │   div bg-chart-3/10 border-      │    │
  │  │   chart-3/30 text-chart-3        │    │
  │  │   [ShieldCheck] "Approved by AM" │    │
  │  │ • deferred:                      │    │
  │  │   div bg-chart-4/10 border-      │    │
  │  │   chart-4/30 text-chart-4        │    │
  │  │   "Deferred by Asset Manager"    │    │
  │  ├─────────────────────────────────┤    │
  │  │ MANAGER-ONLY BUTTONS:            │    │
  │  │ [Button sm "Approve Recommend."] │    │  ← disabled when actionState=approved
  │  │ [Button sm outline "Defer Rec."] │    │  ← disabled when actionState=deferred
  │  │ API: POST /api/ai/recs/{id}/approve│  │  ← Auth: Manager/Admin JWT only
  │  │ API: POST /api/ai/recs/{id}/defer│    │
  │  ├─────────────────────────────────┤    │
  │  │ NON-MANAGER READ-ONLY INDICATOR: │    │
  │  │ <Badge variant="outline">        │    │
  │  │  "Read-only for role: {role}"    │    │  ← shown instead of buttons for Staff
  │  └─────────────────────────────────┘    │     (Staff is /403 at route level anyway)
  │                                         │
  │  Separator                              │
  │  Trace & provenance:                    │
  │  "TRACE & PROVENANCE" text-xs uppercase │
  │  [Database icon]                        │
  │  <AiTracePanel trace={{                 │
  │    source: "predictive_maintenance",    │
  │    filters: "risk>=High; conf>=High",   │
  │    correlation_id, generated_at }} />   │
  └─────────────────────────────────────────┘

**Sub-section: §7.1 AI Recommendation State Machine**
Document the state machine inline:

  State machine (SDD §2.4, lib/predictive.ts):
    pending → approved  (Manager/Admin ONLY — triggers maintenance record creation)
    pending → deferred  (Manager/Admin ONLY — defer_reason optional, 30-day expiry)
    deferred → pending  (re-open after review)
    deferred → expired  (system-auto, 30 days without action)

  PredictiveActionState frontend type = "pending" | "approved" | "deferred"
  (expired is handled as a server-side filter; expired items excluded from GET response)

  SLA constants (lib/predictive.ts):
    HIGH_RISK_SLA_MINUTES = 120  (2-hour window for High-risk pending items)
    slaDueAt set at recommendation creation time (High-risk only)
    getHighRiskSlaState(rec, now) → { countdownMinutes, isOverdue, overdueMinutes }

**Sub-section: Role-action table**

  | Action                          | Admin | Manager | Staff        |
  |---------------------------------|:-----:|:-------:|:-------------|
  | View recommendations            | ✅    | ✅      | 🔒 /403      |
  | See Approve button              | ✅    | ✅      | 🔒 hidden    |
  | See Defer button                | ✅    | ✅      | 🔒 hidden    |
  | Approve via API (RBAC enforced) | ✅    | ✅      | 🔒 403       |
  | View AiTracePanel               | ✅    | ✅      | 🔒 /403      |

Note: "isAssetManager" check in component = `user?.role === "Asset Manager"`.
Admin also has full access (SDD §2.1 — Admin is a superset of Manager for AI actions).
Check: `const canAct = user?.role === "Asset Manager" || user?.role === "Admin"`.

Then append the UX-06 verification marker: <!-- UX-06-VERIFIED -->
  </action>
  <verify>
    <automated>grep -c "UX-06-VERIFIED" .planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</automated>
  </verify>
  <done>WIREFRAMES_2.md contains §7 with: (1) Topbar with /dashboard/ai canonical route annotation, (2) Predictive Summary Card with 3-stat grid, (3) recommendation card ASCII diagram showing High-risk variant (SLA+escalation banner+approval buttons) vs Medium/Low variant (no SLA section), (4) state machine diagram (pending→approved|deferred, deferred→expired), (5) SLA constants (HIGH_RISK_SLA_MINUTES=120), (6) role-action table noting isAssetManager check includes Admin, (7) UX-06-VERIFIED marker.</done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════
     WAVE 2: Notification Center (UX-07) + Audit Log (UX-08)
     Sequential due to single-file ownership; Notifications first, then Audit
     ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" id="18-01-T04">
  <name>Task 18-01-T04 (Wave 2a): §8 Notification Center wireframe — topbar bell + full notifications page (UX-07)</name>
  <files>.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</files>
  <action>
Append `## §8 Notification Center (/dashboard/notifications) (UX-07)` to WIREFRAMES_2.md.
This section has two surfaces: (A) Topbar bell icon modification and (B) full notifications
page. Per IA §1.4 and SDD §1.5, the bell navigates to the FULL PAGE — it does NOT open
a dropdown/popover (see CRITICAL NOTE below).

**CRITICAL NOTE:**
  ⚠ BELL IS NAVIGATION, NOT DROPDOWN
  The bell icon in Topbar MUST navigate to /dashboard/notifications using router.push()
  or a Link href. It must NOT open a Popover, DropdownMenu, or Sheet.
  Reason: "Full page navigation for accessibility and link-shareability." (IA §1.4)
  Anti-pattern: <Popover><PopoverTrigger><Bell /></PopoverTrigger><PopoverContent>...</>
  Correct:      <Button asChild variant="ghost" size="icon"><Link href="/dashboard/notifications">

**Sub-section: §8.1 Topbar Bell Icon (Modification to components/topbar.tsx)**
This subsection documents a required change to an existing component:

  TOPBAR MODIFICATION (add bell icon to existing topbar.tsx):
  ┌────────────────────────────────────────────────────────────────────────────────────┐
  │  TOPBAR — CURRENT STATE (no bell):                                                 │
  │  [≡ mobile menu]  "Page Title"  (caption)           [👤 role badge ▾]             │
  │                                                                                    │
  │  TOPBAR — TARGET STATE (with bell):                                                │
  │  [≡ mobile menu]  "Page Title"  (caption)    [🔔 3]           [👤 role badge ▾]  │
  │                                               ↑ Bell icon                         │
  │                                               ↑ Badge destructive (hidden if 0)   │
  └────────────────────────────────────────────────────────────────────────────────────┘

  Bell icon component spec (add to right-side flex div in topbar.tsx):
    <Button
      variant="ghost"
      size="icon"
      className="relative"
      asChild
    >
      <Link href="/dashboard/notifications">
        <Bell className="size-5" />
        {unreadCount > 0 && (
          <Badge
            variant="destructive"
            className="absolute -top-1 -right-1 h-5 min-w-5 text-xs px-1"
          >
            {unreadCount > 99 ? "99+" : unreadCount}
          </Badge>
        )}
      </Link>
    </Button>

  Data source: unreadCount from useStore() → populated via SSE EventSource("/api/notifications/stream")
  Position: insert before the user avatar button in the right-side flex div
  Badge behavior: Badge is DOM-removed (not hidden) when unreadCount === 0

**Sub-section: §8.2 Full Notifications Page (/dashboard/notifications)**
Route: `/dashboard/notifications`
Roles: Admin, Manager, Staff (all roles; server filters to own notifications)

  TOPBAR:
  ┌────────────────────────────────────────────────────────────────────────────────────┐
  │  "Notification Center"  (h4)   "Your recent alerts and updates"  (caption)        │
  │                                                            [🔔 N]  [👤 ▾]         │
  └────────────────────────────────────────────────────────────────────────────────────┘

  PAGE CONTENT (p-6):
  ┌───────────────────────────────────────────────────────────────────────────────────┐
  │ Card (full-width):                                                                │
  │ ┌───────────────────────────────────────────────────────────────────────────────┐ │
  │ │ CardHeader:                                                                   │ │
  │ │   CardTitle: "Notifications"        CardAction: [Button outline sm "Mark All  │ │
  │ │                                                  Read"]                       │ │
  │ │                                     API: POST /api/notifications/read-all     │ │
  │ ├───────────────────────────────────────────────────────────────────────────────┤ │
  │ │ CardContent:                                                                  │ │
  │ │                                                                               │ │
  │ │  NOTIFICATION ROWS (list, divide-y):                                         │ │
  │ │                                                                               │ │
  │ │  ┌─────────────────────────────────────────────────────────────────────────┐ │ │
  │ │  │ UNREAD ROW:                                                             │ │ │
  │ │  │ flex items-start gap-3 p-3 border-b hover:bg-muted/50                  │ │ │
  │ │  │ ┌────────────┐  ┌────────────────────────────────────────────┐  [●] [→]│ │ │
  │ │  │ │ bg-destr/10│  │ "High failure risk detected"               │  ↑   ↑  │ │ │
  │ │  │ │ rounded-full│  │ text-sm font-medium                        │  │   │  │ │ │
  │ │  │ │ p-1.5      │  │ "Dell Latitude 5540 shows 94% fail prob"   │  │   └─ Link│ │
  │ │  │ │[AlertTriangle│ │ text-xs text-muted-foreground              │  │     href=│ │
  │ │  │ │ text-destr]│  │ "2 min ago" · text-xs text-muted-foreground│  │     deep │ │
  │ │  │ └────────────┘  └────────────────────────────────────────────┘  │     link │ │
  │ │  │                                                                  └─ w-2 h-2│ │ │
  │ │  │                                                                    rounded  │ │ │
  │ │  │                                                                    bg-primary│ │ │
  │ │  │                                                                    (unread  │ │ │
  │ │  │                                                                    dot)     │ │ │
  │ │  └─────────────────────────────────────────────────────────────────────────┘ │ │
  │ │                                                                               │ │
  │ │  ┌─────────────────────────────────────────────────────────────────────────┐ │ │
  │ │  │ READ ROW (no blue dot):                                                 │ │ │
  │ │  │ ┌────────────┐  ┌────────────────────────────────────────────┐      [→]│ │ │
  │ │  │ │ bg-chart-4/ │  │ "Warranty expiry warning"                 │         │ │ │
  │ │  │ │ 10 rounded  │  │ "Dell L7420 warranty expires in 8 days"   │         │ │ │
  │ │  │ │[Shield      │  │ "15 min ago"                              │         │ │ │
  │ │  │ │ text-chart-4│  └────────────────────────────────────────────┘         │ │ │
  │ │  │ └────────────┘                                              no blue dot  │ │ │
  │ │  └─────────────────────────────────────────────────────────────────────────┘ │ │
  │ │                                                                               │ │
  │ │  PAGINATION FOOTER:                                                           │ │
  │ │  ┌─────────────────────────────────────────────────────────────────────────┐ │ │
  │ │  │  "Showing 1–10 of 47 notifications"   [← Prev]  Page 1 of 5  [Next →] │ │ │
  │ │  └─────────────────────────────────────────────────────────────────────────┘ │ │
  │ │  Data: GET /api/notifications?page=N (REST; 10 per page default)             │ │
  │ └───────────────────────────────────────────────────────────────────────────────┘ │
  └───────────────────────────────────────────────────────────────────────────────────┘

**Sub-section: §8.3 Notification Type Reference Table**

Document the 4 notification types with icon, priority, and deep-link target:

  | Type                   | Priority  | Icon                        | Deep-Link Target                           | Roles         |
  |------------------------|-----------|-----------------------------|--------------------------------------------|---------------|
  | high_failure_risk      | 🔴 Crit.  | AlertTriangle text-destr.   | /dashboard/ai?assetId={asset_id}           | Manager, Admin|
  | warranty_expiry_warning| 🟡 Warn.  | Shield text-chart-4         | /dashboard/assets/{asset_id}               | Manager, Admin|
  | upcoming_maintenance   | 🔵 Info   | Wrench text-primary         | /dashboard/maintenance                     | Manager, Staff|
  | overdue_return         | 🔴 Crit.  | Clock text-destructive      | /dashboard/assignments/{assignment_id}     | Manager, Admin|

  Mark-as-read (individual): PATCH /api/notifications/{id}/read → sets is_read=true
  Mark-all-read: POST /api/notifications/read-all → sets all is_read=true for this user
  Unread dot: w-2 h-2 rounded-full bg-primary; DOM-removed (not invisible) when is_read=true

**Sub-section: Role-action table for Notification Center**

  | Action                    | Admin | Manager | Staff             |
  |---------------------------|:-----:|:-------:|:------------------|
  | View /notifications page  | ✅    | ✅      | ✅                |
  | See bell icon in topbar   | ✅    | ✅      | ✅                |
  | See high_failure_risk type| ✅    | ✅      | 🔒 server-filtered|
  | See overdue_return type   | ✅    | ✅      | 🔒 server-filtered|
  | Mark individual read      | ✅    | ✅      | ✅ (own only)     |
  | Mark all read             | ✅    | ✅      | ✅ (own only)     |

Then append the UX-07 verification marker: <!-- UX-07-VERIFIED -->
  </action>
  <verify>
    <automated>grep -c "UX-07-VERIFIED" .planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</automated>
  </verify>
  <done>WIREFRAMES_2.md contains §8 with: (1) CRITICAL NOTE that bell is navigation not dropdown, (2) topbar bell spec with exact component markup (Button+Link+Bell+Badge destructive), (3) full /notifications page layout with Card+CardHeader+CardAction "Mark All Read", (4) unread row anatomy (type icon bg, title/message/timeAgo, blue dot w-2 h-2 bg-primary, deep-link button), (5) read row showing no blue dot, (6) pagination footer, (7) 4-type notification reference table with deep-links, (8) mark-as-read API annotations, (9) role-action table, (10) UX-07-VERIFIED marker.</done>
</task>

<task type="auto" id="18-01-T05">
  <name>Task 18-01-T05 (Wave 2b): §9 Audit Log wireframe — confirms existing implementation, documents immutability rules (UX-08)</name>
  <files>.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</files>
  <action>
Append `## §9 Audit Log (/dashboard/audit) (UX-08)` to WIREFRAMES_2.md. This page is
FULLY IMPLEMENTED at `frontend/app/dashboard/audit/page.tsx`. This section CONFIRMS the
existing design matches UX-08 requirements. Read audit/page.tsx before writing.

Open the section with a confirmation banner:

  > **Implementation Status: CONFIRMED ✅**
  > `/dashboard/audit/page.tsx` is fully implemented and matches all UX-08 requirements.
  > This wireframe section documents the design for reference and confirms immutability
  > constraints. No changes to existing implementation are needed.

**Route and roles header:**
  Route: `/dashboard/audit`
  Roles: Admin ONLY (Manager → /403, Staff → /403)
  VERIFIED: navigation-access.ts — `{ href: "/dashboard/audit", roles: ["Admin"] }`

**Sub-section: Topbar**

  TOPBAR:
  ┌────────────────────────────────────────────────────────────────────────────────────┐
  │  "Audit Log"  (h4)   "Immutable event trail with category filters"  (caption)     │
  │                                                            [🔔 N]  [👤 ▾]         │
  └────────────────────────────────────────────────────────────────────────────────────┘

**Sub-section: Full Page Layout**

  PAGE CONTENT (p-6):
  ┌───────────────────────────────────────────────────────────────────────────────────┐
  │ Card:                                                                             │
  │ ┌───────────────────────────────────────────────────────────────────────────────┐ │
  │ │ CardHeader:  CardTitle: "Audit Event Log"                                     │ │
  │ ├───────────────────────────────────────────────────────────────────────────────┤ │
  │ │ CardContent space-y-4:                                                        │ │
  │ │                                                                               │ │
  │ │  CATEGORY FILTER BAR (flex flex-wrap gap-2):                                 │ │
  │ │  [All ●] [Business] [Security] [AI-assisted]                                 │ │
  │ │   ↑ active = variant="default"  ↑ inactive = variant="outline"  size="sm"   │ │
  │ │  AUDIT_CATEGORIES = ["Business", "Security", "AI-assisted"] (lib/audit-log)  │ │
  │ │  Client-side filter only — no server mutation, no API call on category change│ │
  │ │                                                                               │ │
  │ │  TABLE (overflow-x-auto rounded-md border):                                  │ │
  │ │  ┌──────────┬──────────────────────┬────────────────┬───────────┬────────┬──────────────────┬─────────────────┬──────────┐
  │ │  │ Actor    │ Action               │ Entity         │ Before    │ After  │ Timestamp        │ Correlation ID  │ Details  │
  │ │  ├──────────┼──────────────────────┼────────────────┼───────────┼────────┼──────────────────┼─────────────────┼──────────┤
  │ │  │ Jane Doe │ assignment.approved  │ Assign REQ-300 │ pending   │ active │ 2026-06-28 09:14 │ CORR-2024-0042  │[View det]│
  │ │  ├──────────┼──────────────────────┼────────────────┼───────────┼────────┼──────────────────┼─────────────────┼──────────┤
  │ │  │ System   │ ai.recommendation    │ Asset AST-012  │ pending   │ appr'd │ 2026-06-28 09:02 │ CORR-2024-0041  │[View det]│
  │ │  ├──────────┼──────────────────────┼────────────────┼───────────┼────────┼──────────────────┼─────────────────┼──────────┤
  │ │  │ Admin    │ user.login           │ User USR-007   │ —         │ active │ 2026-06-28 08:55 │ CORR-2024-0039  │[View det]│
  │ │  └──────────┴──────────────────────┴────────────────┴───────────┴────────┴──────────────────┴─────────────────┴──────────┘

  Column details:
    Actor:          event.actor (user display name)  — text-sm
    Action:         event.action (e.g. "assignment.approved")  — text-sm font-mono text-xs
    Entity:         event.entity (e.g. "Assignment REQ-300")  — text-sm
    Before:         event.before (state snapshot string)  — text-sm text-muted-foreground
    After:          event.after (state snapshot string)  — text-sm text-muted-foreground
    Timestamp:      formatDate(event.timestamp) — text-sm
    Correlation ID: event.correlation_id — font-mono text-xs
                    Label: getAuditCorrelationLabel() (from lib/audit-log.ts)
    Details:        <Button variant="outline" size="sm"> "View details" / "Hide details"
                    onClick: toggleExpanded(event.id)  — sets expandedEventId

**Sub-section: §9.1 Expandable Row Detail Panel**
Document the expandable row (verified from audit/page.tsx Fragment pattern):

  EXPANDED ROW (when expandedEventId === event.id):
  ┌───────────────────────────────────────────────────────────────────────────────────┐
  │ <TableRow>                                                                        │
  │   <TableCell colSpan={8}>                                                         │
  │     div className="space-y-2 rounded-md bg-muted/40 p-3":                        │
  │     ┌─────────────────────────────────────────────────────────────────────────┐   │
  │     │ [Badge variant="outline"]{event.category}[/Badge]  ← "Business" etc     │   │
  │     │ "Before: {event.before}"    text-sm                                     │   │
  │     │ "After: {event.after}"      text-sm                                     │   │
  │     │ "Correlation ID: {event.correlation_id}"  text-sm font-mono             │   │
  │     │ "AI Recommendation: {event.aiRecommendation?.summary ?? "None"}"        │   │
  │     └─────────────────────────────────────────────────────────────────────────┘   │
  │   </TableCell>                                                                    │
  │ </TableRow>                                                                       │
  └───────────────────────────────────────────────────────────────────────────────────┘

  Only one row expanded at a time (expandedEventId state — last click toggles).

**Sub-section: §9.2 Immutability Constraints**

  IMMUTABILITY RULES (VERIFIED: SDD §2.5 — AuditEvents table is append-only):
  ✓ No edit button on any audit row — no <DropdownMenu> with "Edit" option
  ✓ No delete button on any audit row — no trash icon, no "Archive" option
  ✓ No "Clear logs" or "Export and delete" actions anywhere on the page
  ✓ No column-click sort — table columns are not sortable (no sort chevrons)
  ✓ Category filter is the ONLY user interaction beyond row expand/collapse
  ✓ Category filter is CLIENT-SIDE only — no server mutation, no PATCH/DELETE API call
  ✗ ANTI-PATTERN: <DropdownMenu> with Edit/Delete in the Actions column → forbidden
  ✗ ANTI-PATTERN: Reusing assets/assignments table pattern (which has edit actions) → forbidden

  Data source: GET /api/audit?category={Business|Security|AI-assisted|All} (REST, Admin JWT)
  Writes: backend-only via event publishers — no frontend write path to AuditEvents table

**Sub-section: Role-access table**

  | Access                    | Admin | Manager | Staff  |
  |---------------------------|:-----:|:-------:|:-------|
  | View /dashboard/audit     | ✅    | 🔒 /403 | 🔒 /403|
  | Filter by category        | ✅    | —       | —      |
  | Expand row details        | ✅    | —       | —      |
  | Any write/edit action     | ✗ N/A | —       | —      |

Then append the UX-08 verification marker: <!-- UX-08-VERIFIED -->
  </action>
  <verify>
    <automated>grep -c "UX-08-VERIFIED" .planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</automated>
  </verify>
  <done>WIREFRAMES_2.md contains §9 with: (1) confirmed implementation status banner, (2) category filter bar (All/Business/Security/AI-assisted with default/outline variant logic), (3) 8-column table ASCII diagram (Actor/Action/Entity/Before/After/Timestamp/Correlation ID/Details), (4) expandable row with colSpan=8 muted panel showing category badge + before/after + correlation_id + AI recommendation summary, (5) immutability constraints checklist (no edit/delete/sort), (6) role-access table (Admin-only), (7) UX-08-VERIFIED marker.</done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════
     WAVE 3: User Management (UX-09)
     ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" id="18-01-T06">
  <name>Task 18-01-T06 (Wave 3): §10 User Management wireframe — user list, create/edit form, deactivate dialog (UX-09)</name>
  <files>.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</files>
  <action>
Append `## §10 User Management (/dashboard/users) (UX-09)` to WIREFRAMES_2.md. This is a
NEW page with no existing implementation. The pattern follows AssetFormDialog for form,
Table for list, and Dialog for deactivate confirmation.

**Route and roles header:**
  Routes: `/dashboard/users` (list), `/dashboard/users/new` (create), `/dashboard/users/[id]/edit` (edit)
  Roles: Admin ONLY (Manager → /403, Staff → /403)
  VERIFIED: navigation-access.ts — `{ href: "/dashboard/users", roles: ["Admin"] }`
  VERIFIED: SDD §2.1 Permission Matrix — "Create/edit users: Admin Full, Manager —, Staff —"

**Sub-section: §10.1 User List Page (/dashboard/users)**

  TOPBAR:
  ┌────────────────────────────────────────────────────────────────────────────────────┐
  │  "User Management"  (h4)   "Administrator only"  (caption)                        │
  │                                                            [🔔 N]  [👤 ▾]         │
  └────────────────────────────────────────────────────────────────────────────────────┘

  PAGE CONTENT (p-6):
  ┌───────────────────────────────────────────────────────────────────────────────────┐
  │ Card:                                                                             │
  │ ┌───────────────────────────────────────────────────────────────────────────────┐ │
  │ │ CardHeader:                                                                   │ │
  │ │   CardTitle: "Users"                                                          │ │
  │ │   CardAction: [Button asChild] <Link href="/dashboard/users/new">             │ │
  │ │               <Plus className="size-4" />  "Add User"  [/Link][/Button]      │ │
  │ ├───────────────────────────────────────────────────────────────────────────────┤ │
  │ │ CardContent space-y-4:                                                        │ │
  │ │                                                                               │ │
  │ │  SEARCH BAR (flex gap-2):                                                     │ │
  │ │  ┌──────────────────────────────────────────────────────────────────────┐    │ │
  │ │  │ [🔍 Search by name or email...]  ← <Input> type="search"             │    │ │
  │ │  └──────────────────────────────────────────────────────────────────────┘    │ │
  │ │  Data: client-side filter on GET /api/users response                          │ │
  │ │                                                                               │ │
  │ │  TABLE:                                                                       │ │
  │ │  ┌────────────────┬──────────────────────────────┬─────────────────┬──────────────┬────────────┬────────────┐
  │ │  │ Name           │ Email                        │ Role            │ Department   │ Status     │ Actions    │
  │ │  ├────────────────┼──────────────────────────────┼─────────────────┼──────────────┼────────────┼────────────┤
  │ │  │ Jane Admin     │ jane@company.com             │ [Admin]         │ IT           │ [Active]   │     ⋯      │
  │ │  ├────────────────┼──────────────────────────────┼─────────────────┼──────────────┼────────────┼────────────┤
  │ │  │ Bob Manager    │ bob@company.com              │ [Asset Manager] │ Operations   │ [Active]   │     ⋯      │
  │ │  ├────────────────┼──────────────────────────────┼─────────────────┼──────────────┼────────────┼────────────┤
  │ │  │ Alice Staff    │ alice@company.com            │ [Staff]         │ Warehouse    │ [Active]   │     ⋯      │
  │ │  ├────────────────┼──────────────────────────────┼─────────────────┼──────────────┼────────────┼────────────┤
  │ │  │ Tom Former     │ tom@company.com              │ [Staff]         │ Sales        │ [Inactive] │     ⋯      │
  │ │  │ (opacity-60 row)│                             │                 │              │            │            │
  │ │  └────────────────┴──────────────────────────────┴─────────────────┴──────────────┴────────────┴────────────┘

  Role column badge mapping (VERIFIED: 18-RESEARCH.md §User Management Architecture):
    Admin:         <Badge variant="default">Admin</Badge>          ← blue pill
    Asset Manager: <Badge variant="secondary">Asset Manager</Badge> ← subtle bg
    Staff:         <Badge variant="outline">Staff</Badge>           ← outline

  Status column badge:
    Active:   <Badge variant="outline" className="text-foreground">Active</Badge>
    Inactive: <Badge variant="outline" className="text-muted-foreground">Inactive</Badge>
    Inactive rows: className="opacity-60" on <TableRow>

  Actions column (⋯ DropdownMenu):
    <DropdownMenuItem asChild><Link href={`/dashboard/users/${id}/edit`}>Edit</Link></DropdownMenuItem>
    <DropdownMenuSeparator />
    <DropdownMenuItem
      className="text-destructive focus:text-destructive"
      onClick={() => openDeactivateDialog(user)}
    >
      Deactivate
    </DropdownMenuItem>
    NOTE: "Deactivate" text only (no "Delete" option anywhere — see §10.3 constraints)
    NOTE: Show "Reactivate" instead of "Deactivate" when user.is_active === false

  Data source: GET /api/users (REST, Admin JWT required — server returns 403 for other roles)

**Sub-section: §10.2 Create User Page (/dashboard/users/new)**

  TOPBAR:
  ┌────────────────────────────────────────────────────────────────────────────────────┐
  │  "Add User"  (h4)   "Create a new system account"  (caption)                      │
  │                                                            [🔔 N]  [👤 ▾]         │
  └────────────────────────────────────────────────────────────────────────────────────┘

  PAGE CONTENT (p-6, max-w-lg mx-auto):
  ┌────────────────────────────────────────────────────────────────────────────┐
  │ Card:                                                                      │
  │ ┌──────────────────────────────────────────────────────────────────────┐  │
  │ │ CardHeader: CardTitle "New User"  CardDescription "Fill in details"  │  │
  │ ├──────────────────────────────────────────────────────────────────────┤  │
  │ │ CardContent space-y-4:                                               │  │
  │ │                                                                      │  │
  │ │  Name * ─────────────────────────────────────────────────────        │  │
  │ │  ┌────────────────────────────────────────────────────────────┐     │  │
  │ │  │ Full name                            (Input, required)     │     │  │
  │ │  └────────────────────────────────────────────────────────────┘     │  │
  │ │                                                                      │  │
  │ │  Email * ────────────────────────────────────────────────────        │  │
  │ │  ┌────────────────────────────────────────────────────────────┐     │  │
  │ │  │ email@company.com                    (Input type="email")  │     │  │
  │ │  └────────────────────────────────────────────────────────────┘     │  │
  │ │                                                                      │  │
  │ │  Role * ─────────────────────────────────────────────────────        │  │
  │ │  ┌────────────────────────────────────────────────────────────┐     │  │
  │ │  │ Select role...                                    ▾ Select │     │  │
  │ │  │   Admin / Asset Manager / Staff                            │     │  │
  │ │  └────────────────────────────────────────────────────────────┘     │  │
  │ │                                                                      │  │
  │ │  Department * ───────────────────────────────────────────────        │  │
  │ │  ┌────────────────────────────────────────────────────────────┐     │  │
  │ │  │ Department name                      (Input, required)     │     │  │
  │ │  └────────────────────────────────────────────────────────────┘     │  │
  │ │                                                                      │  │
  │ │  Password * ────────────────────────────────────────────────         │  │
  │ │  ┌────────────────────────────────────────────────────────────┐     │  │
  │ │  │ ••••••••••                          (Input type="password")│     │  │
  │ │  └────────────────────────────────────────────────────────────┘     │  │
  │ │  Note: Password field on CREATE form only — absent on edit form      │  │
  │ │                                                                      │  │
  │ ├──────────────────────────────────────────────────────────────────────┤  │
  │ │ CardFooter (flex justify-end gap-2):                                 │  │
  │ │  [Button variant="outline" asChild]<Link href="/dashboard/users">    │  │
  │ │    Cancel</Link>[/Button]                                            │  │
  │ │  [Button type="submit"]  Save User  [/Button]                        │  │
  │ │  API: POST /api/users (Admin JWT required)                           │  │
  │ └──────────────────────────────────────────────────────────────────────┘  │
  └────────────────────────────────────────────────────────────────────────────┘

**Sub-section: §10.2b Edit User Page (/dashboard/users/[id]/edit)**
Same layout as Create, with these differences:
  - Topbar: "Edit User" title
  - Fields pre-populated from GET /api/users/{id} response
  - Password field is ABSENT (use separate "Change Password" flow if needed — not in v1.2 scope)
  - Additional "Account Status" toggle:
    ┌────────────────────────────────────────────────────────────────────────┐
    │  Account Status ─────────────────────────────────────────────────────  │
    │  ┌──────────────────────────────────────────────────────────────────┐  │
    │  │ [Switch] Active                                                  │  │
    │  │  toggle off = is_active: false  (reactivate: toggle on)          │  │
    │  └──────────────────────────────────────────────────────────────────┘  │
    └────────────────────────────────────────────────────────────────────────┘
  - Save button: PATCH /api/users/{id} (Admin JWT required)

**Sub-section: §10.3 Deactivate Confirmation Dialog**
Triggered from ⋯ → "Deactivate" in user list. Uses mandatory Dialog per DESIGN_SYSTEM §4.8:

  DEACTIVATE DIALOG:
  ┌────────────────────────────────────────────────────────────┐
  │  Dialog (open=isDeactivateDialogOpen):                     │
  │  ┌──────────────────────────────────────────────────────┐  │
  │  │ DialogHeader:                                        │  │
  │  │   DialogTitle: "Deactivate User"                     │  │
  │  │   DialogDescription:                                 │  │
  │  │     "{userName} will no longer be able to log in.    │  │
  │  │      This action can be reversed."                   │  │
  │  ├──────────────────────────────────────────────────────┤  │
  │  │ DialogFooter:                                        │  │
  │  │  [Button variant="outline"]Cancel[/Button]           │  │
  │  │  [Button variant="destructive"]Deactivate[/Button]   │  │
  │  │  API: PATCH /api/users/{id}  { is_active: false }    │  │
  │  └──────────────────────────────────────────────────────┘  │
  └────────────────────────────────────────────────────────────┘

**Sub-section: §10.4 No-Hard-Delete Constraint**

  HARD-DELETE PROHIBITION (VERIFIED: SDD §2.5 ER, 18-RESEARCH.md §Anti-Patterns):
  ✗ No "Delete" menu item in the Actions DropdownMenu — EVER
  ✗ No "Permanently Delete" button on edit form
  ✗ No DELETE /api/users/{id} API call from frontend
  ✗ No trash icon (🗑) on user rows
  ✓ Only soft-delete: PATCH /api/users/{id} { is_active: false }
  ✓ Deactivated users remain visible in the list with opacity-60 + Inactive badge
  ✓ Reactivate is available via edit form Account Status toggle (is_active: true)

  Reason: User records are referenced by AuditEvents.actor — deleting a user would break
  audit trail integrity. The append-only AuditEvents table requires user records to persist.

**Sub-section: Role-action table**

  | Action                         | Admin | Manager | Staff  |
  |--------------------------------|:-----:|:-------:|:-------|
  | View /dashboard/users          | ✅    | 🔒 /403 | 🔒 /403|
  | Create user (POST /api/users)  | ✅    | 🔒 403  | 🔒 403 |
  | Edit user (PATCH /api/users)   | ✅    | 🔒 403  | 🔒 403 |
  | Deactivate (is_active=false)   | ✅    | 🔒 403  | 🔒 403 |
  | Hard-delete                    | ✗ N/A | —       | —      |

Then append the UX-09 verification marker: <!-- UX-09-VERIFIED -->

Close WIREFRAMES_2.md with a final section:

  ---
  ## Document Status

  | Section | Requirement | Status |
  |---------|-------------|--------|
  | §6 IoT Monitoring   | UX-05 | ✅ Complete |
  | §7 AI Predictive    | UX-06 | ✅ Complete |
  | §8 Notification Ctr | UX-07 | ✅ Complete |
  | §9 Audit Log        | UX-08 | ✅ Complete (confirms existing implementation) |
  | §10 User Management | UX-09 | ✅ Complete |

  *Next phase (Phase 19): Implement pages per these wireframes.*
  *Implementation priority: /dashboard/ai (route migration) → /dashboard/iot → /dashboard/notifications → /dashboard/users*
  </action>
  <verify>
    <automated>grep -c "UX-09-VERIFIED" .planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</automated>
  </verify>
  <done>WIREFRAMES_2.md contains §10 with: (1) user list with table (Name/Email/Role/Department/Status/Actions columns), (2) role badge mapping (Admin=default/blue, Asset Manager=secondary, Staff=outline), (3) Inactive rows with opacity-60, (4) Actions dropdown showing Deactivate (not Delete), (5) Create form with 5 fields (Name/Email/Role/Department/Password), (6) Edit form without Password field + Account Status toggle for reactivate, (7) Deactivate Dialog with "This action can be reversed" text and destructive button, (8) no-hard-delete constraints checklist, (9) role-action table, (10) UX-09-VERIFIED marker, (11) Document Status table at end.</done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════
     WAVE 4: Verification — all 5 section markers present
     ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" id="18-01-T07">
  <name>Task 18-01-T07 (Wave 4): Verify WIREFRAMES_2.md completeness — all 5 UX markers + key content anchors</name>
  <files>.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md</files>
  <action>
Run the verification checks below. If any check fails, edit WIREFRAMES_2.md to add the
missing content before marking the plan complete. All checks must pass.

Verification script to execute:

  WIRES=".planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md"

  echo "=== UX requirement markers ==="
  grep -c "UX-05-VERIFIED" $WIRES && echo "UX-05 ✓" || echo "UX-05 ✗ MISSING"
  grep -c "UX-06-VERIFIED" $WIRES && echo "UX-06 ✓" || echo "UX-06 ✗ MISSING"
  grep -c "UX-07-VERIFIED" $WIRES && echo "UX-07 ✓" || echo "UX-07 ✗ MISSING"
  grep -c "UX-08-VERIFIED" $WIRES && echo "UX-08 ✓" || echo "UX-08 ✗ MISSING"
  grep -c "UX-09-VERIFIED" $WIRES && echo "UX-09 ✓" || echo "UX-09 ✗ MISSING"

  echo "=== Route migration TODO present ==="
  grep -c "Route Migration TODO" $WIRES && echo "TODO ✓" || echo "TODO ✗ MISSING"

  echo "=== IoT: sensor category table present ==="
  grep -c "SENSOR_CATEGORY_MAP\|Sensor Tile Grid" $WIRES && echo "IoT tiles ✓" || echo "IoT tiles ✗ MISSING"

  echo "=== IoT: connection status Badge documented ==="
  grep -c "Reconnecting\|Disconnected\|bg-chart-3/15" $WIRES && echo "Conn status ✓" || echo "Conn status ✗ MISSING"

  echo "=== AI: SLA countdown present ==="
  grep -c "getHighRiskSlaState\|HIGH_RISK_SLA_MINUTES\|formatSlaCountdown" $WIRES && echo "SLA ✓" || echo "SLA ✗ MISSING"

  echo "=== AI: Manager-only approval gate ==="
  grep -c "Approve Recommendation\|canAct\|isAssetManager" $WIRES && echo "Approval ✓" || echo "Approval ✗ MISSING"

  echo "=== Notifications: bell is navigation NOT dropdown ==="
  grep -c "BELL IS NAVIGATION\|NOT a dropdown\|NOT open a Popover" $WIRES && echo "Bell nav ✓" || echo "Bell nav ✗ MISSING"

  echo "=== Notifications: 4 type rows in reference table ==="
  grep -c "high_failure_risk\|warranty_expiry_warning\|upcoming_maintenance\|overdue_return" $WIRES
  # Should return count >= 4

  echo "=== Audit: colSpan=8 expandable row ==="
  grep -c "colSpan.*8\|colSpan={8}" $WIRES && echo "Audit expand ✓" || echo "Audit expand ✗ MISSING"

  echo "=== Audit: immutability — no edit/delete ==="
  grep -c "append-only\|No edit button\|No delete button" $WIRES && echo "Immutable ✓" || echo "Immutable ✗ MISSING"

  echo "=== Users: no hard-delete constraint ==="
  grep -c "No.*Delete\|hard-delete\|is_active.*false" $WIRES && echo "No-delete ✓" || echo "No-delete ✗ MISSING"

  echo "=== Users: deactivate dialog present ==="
  grep -c "Deactivate User\|This action can be reversed" $WIRES && echo "Dialog ✓" || echo "Dialog ✗ MISSING"

  echo "=== Document Status table present ==="
  grep -c "Document Status" $WIRES && echo "Status table ✓" || echo "Status table ✗ MISSING"

  echo "=== All sections present ==="
  grep -c "§6 IoT Monitoring\|§7 AI Predictive\|§8 Notification\|§9 Audit Log\|§10 User Management" $WIRES
  # Should return 5
  </action>
  <verify>
    <automated>
      WIRES=".planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md"
      ALL_PASS=true
      for MARKER in "UX-05-VERIFIED" "UX-06-VERIFIED" "UX-07-VERIFIED" "UX-08-VERIFIED" "UX-09-VERIFIED" "Route Migration TODO" "SENSOR_CATEGORY_MAP\|Sensor Tile Grid" "getHighRiskSlaState\|HIGH_RISK_SLA_MINUTES" "Approve Recommendation" "BELL IS NAVIGATION\|NOT a dropdown" "colSpan.*8\|colSpan={8}" "No.*Delete\|hard-delete" "Deactivate User" "Document Status"; do
        if ! grep -qE "$MARKER" $WIRES 2>/dev/null; then
          echo "MISSING: $MARKER"
          ALL_PASS=false
        fi
      done
      $ALL_PASS && echo "ALL CHECKS PASSED" || echo "SOME CHECKS FAILED — fix before marking complete"
    </automated>
  </verify>
  <done>All 14 content checks pass: 5 UX verification markers present (UX-05 through UX-09), route migration TODO documented, IoT sensor tile grid + connection status present, AI SLA + approval gate referenced, bell navigation (not dropdown) noted, audit colSpan=8 expandable row documented, audit immutability + no-edit confirmed, user management no-hard-delete + deactivate dialog documented, Document Status table at end of file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → API | All AI approval, user CRUD, and notification mark-read actions cross this boundary |
| Role check in UI → API RBAC | Frontend role check (isAssetManager, Admin-only routes) is UX convenience only; server enforces RBAC independently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | POST /api/ai/recommendations/{id}/approve | mitigate | API middleware enforces `role IN (Admin, Asset Manager)` — frontend role check is not the security boundary (wireframe documents this explicitly in §7) |
| T-18-02 | Elevation of Privilege | GET/POST /api/users | mitigate | Server verifies Admin JWT on every request; 403 for Manager/Staff even if they navigate directly (wireframe documents Admin-only access in §10) |
| T-18-03 | Tampering | PATCH /api/users/{id} (deactivate) | mitigate | Only is_active field writable via this endpoint; no hard-delete path exists in frontend wireframe or API spec |
| T-18-04 | Tampering | AuditEvents table | accept | Append-only at DB level (SDD §2.5); frontend has no write path — wireframe prohibits all edit/delete UI elements on audit page |
| T-18-05 | Information Disclosure | GET /api/notifications | mitigate | Server filters to requesting user's own notifications; high_failure_risk/overdue_return types filtered to Manager/Admin server-side |
| T-18-06 | Spoofing | Bell icon unread badge count | accept | Unread count sourced from authenticated SSE stream; no PII in badge number; low-value target |
| T-18-SC | Tampering | No npm/pip installs in this phase | accept | Phase 18 is documentation-only; no package-manager installs — slopcheck not applicable |
</threat_model>

<verification>
Phase 18 plan complete when:

1. WIREFRAMES_2.md exists at `.planning/phases/18-iot-ai-ui-wireframes/WIREFRAMES_2.md`
2. All 5 UX verification markers present: `grep -E "UX-0[5-9]-VERIFIED" WIREFRAMES_2.md | wc -l` returns 5
3. Route migration TODO documents both the create-ai-page and add-redirect steps
4. IoT sensor category table shows correct tile counts (Laptop 5, Printer 6, Forklift 5, Monitor/OE 4)
5. AI section references getHighRiskSlaState and formatSlaCountdown from lib/predictive.ts
6. Notification bell spec says "navigate" not "dropdown" / "popover"
7. Audit section documents colSpan=8 expandable row and append-only immutability
8. User management section contains deactivate dialog with "This action can be reversed" and prohibits hard-delete
9. Document Status table lists all 5 sections as Complete
</verification>

<success_criteria>
WIREFRAMES_2.md is complete and authoritative when:

- IoT Monitoring wireframe covers the 2-panel layout, per-category sensor tile count table
  (Laptop:5, Printer:6, Forklift:5, Monitor:4, OE:4), connection status Badge with 3 states,
  LineChart+ReferenceLine per active sensor, and chart color→sensor mapping — all annotated
  with data sources (WS /api/iot/{assetId}/stream for live, REST for historical).

- AI Predictive wireframe covers the Predictive Summary Card, High-risk card (SLA section
  with Clock3+formatSlaCountdown+escalation banner+approval buttons) vs Medium/Low card
  (no SLA section), Manager/Admin role gate for Approve/Defer, read-only Badge for other
  roles, and the state machine (pending→approved|deferred→expired).

- Notification Center wireframe covers the topbar bell icon modification (Bell+Badge in
  topbar.tsx, navigates full-page to /notifications — NOT a dropdown), full /notifications
  page layout (notification rows with type icon + unread dot + deep-link + mark-as-read +
  mark-all-read + pagination), and the 4 notification type reference table with deep-links.

- Audit Log wireframe confirms existing /dashboard/audit implementation — category filter,
  8-column immutable table, expandable row with colSpan=8 muted detail panel — and
  documents append-only constraints (no edit/delete/sort UI elements, ever).

- User Management wireframe covers Admin-only user list with role badge mapping, create form
  (Name/Email/Role/Department/Password), edit form (same minus Password + Account Status
  toggle for reactivate), Deactivate Dialog with "This action can be reversed", and the
  hard-delete prohibition constraint (no DELETE API call, no trash icon, no Permanently
  Delete button — PATCH is_active=false only).

- All 5 requirements (UX-05 through UX-09) are verifiable via grep on marker strings.
</success_criteria>

<output>
Create `.planning/phases/18-iot-ai-ui-wireframes/18-01-SUMMARY.md` when done.

Summary must include:
- Confirmation WIREFRAMES_2.md was created
- Line count of the file
- All 5 UX markers confirmed present
- Any sections that needed revision after T07 verification run
- Known gaps or decisions deferred to Phase 19 implementation
</output>
