---
phase: 10-reporting-audit-log-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - v0-ai-asset-management/lib/audit-log.ts
  - v0-ai-asset-management/lib/audit-log.test.ts
autonomous: true
requirements: [AUDT-01]
must_haves:
  truths:
    - "Audit log has a typed immutable event dataset containing actor, action, entity, before/after state, timestamp, and correlation_id."
    - "Audit categories are modeled as Business, Security, and AI-assisted."
    - "Audit event payload supports details required for later row expansion."
  artifacts:
    - path: "v0-ai-asset-management/lib/audit-log.ts"
      provides: "AuditEvent type contract, immutable seed dataset, category constants, read-only selectors"
    - path: "v0-ai-asset-management/lib/audit-log.test.ts"
      provides: "Contract tests for required fields, category validity, and read-only behavior"
  key_links:
    - from: "lib/audit-log.ts"
      to: "app/dashboard/audit/page.tsx"
      via: "exported typed selectors and constants"
      pattern: "export\\s+(type\\s+AuditEvent|getAuditEvents|AUDIT_CATEGORIES)"
---

<objective>
Define the immutable audit event data contract and selector layer used by the audit UI.

Purpose: Establish a reliable typed foundation for AUDT requirements before UI wiring.
Output: `lib/audit-log.ts` with immutable dataset and tested selectors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/10-reporting-audit-log-ui/10-CONTEXT.md
@.planning/phases/10-reporting-audit-log-ui/10-RESEARCH.md
@v0-ai-asset-management/lib/data.ts
@v0-ai-asset-management/lib/ai-governance.ts

<interfaces>
From `lib/ai-governance.ts`:
```ts
export const CORRELATION_LABEL = "Correlation ID"
```

Required audit contract for downstream plan:
```ts
type AuditCategory = "Business" | "Security" | "AI-assisted"

type AuditEvent = {
  id: string
  category: AuditCategory
  actor: string
  action: string
  entity: string
  before: string
  after: string
  timestamp: string
  correlation_id: string
  aiRecommendation?: { id: string; summary: string } | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit log interface contract and immutable seed skeleton</name>
  <files>v0-ai-asset-management/lib/audit-log.ts</files>
  <action>Create `AuditCategory` and `AuditEvent` exports, `AUDIT_CATEGORIES`, and a seed dataset declared immutable/read-only (per D-03 read-only immutability decision). Include fields needed for AUDT-01 and later expansion details in AUDT-03, including before/after and AI linkage metadata.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <done>Typed audit contract exists with immutable seed events and category constants for Business/Security/AI-assisted (D-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tested selector helpers for read-only audit consumption</name>
  <files>v0-ai-asset-management/lib/audit-log.ts, v0-ai-asset-management/lib/audit-log.test.ts</files>
  <behavior>
    - Test 1: every event contains AUDT-01 required columns including correlation_id.
    - Test 2: category values are restricted to Business/Security/AI-assisted (D-04).
    - Test 3: selector returns read-only dataset and does not expose mutation APIs (D-03).
  </behavior>
  <action>Add read-only retrieval/filter helper exports (no create/update/delete functions) and lock behavior with Vitest. Keep output shaped for table and expansion consumers without introducing persistence/API mutation paths (deferred).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- lib/audit-log.test.ts</automated>
  </verify>
  <done>Audit data layer is immutable, category-constrained, and UI-ready for table/filter/expand rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed event contract -> UI consumers | Incorrect contract can break audit integrity display |
| Audit category data -> filter controls | Invalid category values can bypass filter semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | T | `lib/audit-log.ts` seed and selectors | mitigate | Expose read-only selector APIs only; no mutation methods (D-03). |
| T-10-05 | R | audit event fields | mitigate | Require typed required fields (actor/action/entity/before/after/timestamp/correlation_id) via tests for AUDT-01. |
| T-10-06 | I | category model | mitigate | Constrain category union to Business/Security/AI-assisted and validate in tests (D-04). |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npm test -- lib/audit-log.test.ts`
- `cd v0-ai-asset-management && npx tsc --noEmit`
</verification>

<success_criteria>
- Typed immutable audit event contract exists and passes tests.
- Dataset includes all AUDT-01 fields and category taxonomy for AUDT-02.
- No audit mutation surfaces exist in data layer.
</success_criteria>

<output>
After completion, create `.planning/phases/10-reporting-audit-log-ui/10-02-SUMMARY.md`
</output>
