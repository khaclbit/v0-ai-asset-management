---
phase: 10-reporting-audit-log-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - v0-ai-asset-management/lib/reporting.ts
  - v0-ai-asset-management/lib/reporting.test.ts
  - v0-ai-asset-management/app/dashboard/reports/page.tsx
  - v0-ai-asset-management/app/dashboard/reports/page.test.tsx
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]
must_haves:
  truths:
    - "Reports page shows asset overview with counts by category and lifecycle state."
    - "Reports page shows assignment report split into active and historical."
    - "Reports page shows maintenance report split into upcoming and overdue."
    - "Staff user sees only assignment rows where assignee equals current user, while Auditor sees full report visibility."
  artifacts:
    - path: "v0-ai-asset-management/lib/reporting.ts"
      provides: "Typed report selectors for asset, assignment, and maintenance slices with role-scoped assignment filtering"
    - path: "v0-ai-asset-management/app/dashboard/reports/page.tsx"
      provides: "UI sections/cards/tables for all three report families"
    - path: "v0-ai-asset-management/app/dashboard/reports/page.test.tsx"
      provides: "UI behavior coverage for required report sections and staff scope guard"
  key_links:
    - from: "app/dashboard/reports/page.tsx"
      to: "lib/reporting.ts"
      via: "selector calls for report aggregates"
      pattern: "build.*Report|scope.*Assignment"
    - from: "lib/reporting.ts"
      to: "useStore().user + assignmentRecords"
      via: "staff scope predicate (assignee === current user)"
      pattern: "assignee\\s*===\\s*currentUserName"
---

<objective>
Deliver complete Phase 10 reports coverage with role-correct assignment scoping.

Purpose: Close RPT-01..04 functional gaps identified in milestone audit while honoring locked role decisions.
Output: Typed reporting selectors + fully wired reports page + regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/10-reporting-audit-log-ui/10-CONTEXT.md
@.planning/phases/10-reporting-audit-log-ui/10-RESEARCH.md
@v0-ai-asset-management/app/dashboard/reports/page.tsx
@v0-ai-asset-management/lib/store.tsx
@v0-ai-asset-management/lib/data.ts

<interfaces>
From `lib/store.tsx`:
```ts
user: { name: string; role: UserRole } | null
assets: Asset[]
assignmentRecords: AssignmentRecord[]
maintenanceRecords: MaintenanceRecord[]
```

From `lib/data.ts`:
```ts
export type UserRole = "Admin" | "Asset Manager" | "Staff" | "Auditor"
export type AssignmentRecord = { assignee: string; status: AssignmentStatus; dueDate: string; returnDate: string | null; ... }
export type Asset = { category: AssetCategory; status: AssetStatus; ... }
export type MaintenanceRecord = { status: MaintenanceStatus; scheduledDate: string; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create typed reporting selectors with role scope enforcement</name>
  <files>v0-ai-asset-management/lib/reporting.ts, v0-ai-asset-management/lib/reporting.test.ts</files>
  <behavior>
    - Test 1: asset overview returns count-by-category and count-by-lifecycle-state for RPT-01.
    - Test 2: assignment report returns active and historical partitions for RPT-02.
    - Test 3: staff scope returns only records where `assignee === current user` (D-01), while Auditor receives full assignment dataset (D-02).
    - Test 4: maintenance report returns upcoming and overdue partitions for RPT-03.
  </behavior>
  <action>Implement pure selector helpers for report data composition and role scoping. Enforce Staff assignment-only visibility with strict predicate per D-01; keep Auditor fully visible per D-02 even if broader filtering patterns suggest otherwise. Do not add persistence/API calls (deferred idea).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- lib/reporting.test.ts</automated>
  </verify>
  <done>Selectors are typed, deterministic, and encode D-01/D-02 role rules for report data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild Reports page to consume selectors and render all required report sections</name>
  <files>v0-ai-asset-management/app/dashboard/reports/page.tsx, v0-ai-asset-management/app/dashboard/reports/page.test.tsx</files>
  <behavior>
    - Test 1: page renders asset overview sections with category and lifecycle counts (RPT-01).
    - Test 2: page renders assignment active/historical sections (RPT-02).
    - Test 3: page renders maintenance upcoming/overdue sections (RPT-03).
    - Test 4: staff view only shows self-assigned records while Auditor view shows broader dataset (RPT-04, D-01, D-02).
  </behavior>
  <action>Refactor reports route to consume `lib/reporting.ts` outputs and render all required sections using existing dashboard UI patterns. Apply staff-only assignment scoping in UI state flow per D-01 and keep auditor full visibility per D-02. Keep implementation read-only and mock-data-only to stay in Phase 10 scope.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- app/dashboard/reports/page.test.tsx</automated>
  </verify>
  <done>Reports route fully satisfies RPT-01..04 with role-correct assignment visibility behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store role/user context -> report selectors | Untrusted client role context drives visibility |
| In-memory datasets -> report UI | Incorrect filtering could expose unauthorized assignment records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I | `lib/reporting.ts` staff scope selector | mitigate | Enforce explicit `assignee === current user` predicate for Staff per D-01 and lock with tests. |
| T-10-02 | E | `app/dashboard/reports/page.tsx` role branch logic | mitigate | Apply role-aware selector branch for Staff vs Auditor/Admin/Asset Manager and verify in page tests (D-01, D-02). |
| T-10-03 | T | report aggregate helpers | mitigate | Keep helpers pure/read-only and avoid state mutation or side effects in report composition. |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npm test -- lib/reporting.test.ts app/dashboard/reports/page.test.tsx`
- `cd v0-ai-asset-management && npm run build`
</verification>

<success_criteria>
- RPT-01, RPT-02, RPT-03, RPT-04 are all demonstrably satisfied in UI and tests.
- Staff assignment visibility is constrained to own records only.
- Auditor report visibility remains full.
</success_criteria>

<output>
After completion, create `.planning/phases/10-reporting-audit-log-ui/10-01-SUMMARY.md`
</output>
