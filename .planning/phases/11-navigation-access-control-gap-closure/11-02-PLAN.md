---
phase: 11-navigation-access-control-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - v0-ai-asset-management/app/dashboard/layout.tsx
  - v0-ai-asset-management/app/dashboard/layout.test.tsx
  - v0-ai-asset-management/app/page.test.tsx
  - v0-ai-asset-management/components/sidebar-auth.test.tsx
autonomous: true
requirements: [FNDN-03, FNDN-04, FNDN-06]
must_haves:
  truths:
    - "Unauthorized direct URL access to restricted dashboard modules is denied and redirected to /dashboard with access-denied feedback."
    - "Login still routes users into dashboard flow."
    - "Logout still returns users to login."
  artifacts:
    - path: "v0-ai-asset-management/app/dashboard/layout.tsx"
      provides: "Route-level RBAC guard layered on existing auth guard"
    - path: "v0-ai-asset-management/app/dashboard/layout.test.tsx"
      provides: "Guard behavior regression coverage for allowed/denied routes"
    - path: "v0-ai-asset-management/app/page.test.tsx"
      provides: "Login redirect regression coverage"
    - path: "v0-ai-asset-management/components/sidebar-auth.test.tsx"
      provides: "Logout behavior regression coverage"
  key_links:
    - from: "app/dashboard/layout.tsx"
      to: "lib/navigation-access.ts"
      via: "canAccessDashboardRoute(user.role, pathname)"
      pattern: "canAccessDashboardRoute"
    - from: "denied route branch"
      to: "/dashboard"
      via: "router.replace + access denied feedback"
      pattern: "router\\.replace\\(\"/dashboard\"\\)"
---

<objective>
Add route-level RBAC enforcement in dashboard layout and verify login/logout/navigation behavior remains intact.

Purpose: Close direct-URL bypass gap without regressing existing auth flow.
Output: Layout guard + denial feedback + auth/navigation regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-navigation-access-control-gap-closure/11-CONTEXT.md
@.planning/phases/11-navigation-access-control-gap-closure/11-RESEARCH.md
@.planning/phases/11-navigation-access-control-gap-closure/11-UI-SPEC.md
@v0-ai-asset-management/app/dashboard/layout.tsx
@v0-ai-asset-management/app/page.tsx
@v0-ai-asset-management/components/sidebar.tsx
@v0-ai-asset-management/lib/navigation-access.ts

<interfaces>
From `lib/navigation-access.ts` (Plan 11-01 contract):
```ts
export function canAccessDashboardRoute(role: UserRole, pathname: string): boolean
export function isKnownDashboardRoute(pathname: string): boolean
```

From `lib/store.tsx`:
```ts
user: { role: UserRole } | null
logout: () => void
login: (email: string, role: UserRole) => void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce layout-level route RBAC with redirect feedback</name>
  <files>v0-ai-asset-management/app/dashboard/layout.tsx, v0-ai-asset-management/app/dashboard/layout.test.tsx</files>
  <behavior>
    - Test 1: unauthenticated users are still redirected to `/` (existing behavior unchanged, D-05).
    - Test 2: authenticated but unauthorized route access redirects to `/dashboard` and triggers access-denied feedback (D-04).
    - Test 3: authorized roles can load allowed routes without redirect loop (D-03).
  </behavior>
  <action>Extend dashboard layout to use `usePathname()` with shared access helper checks from Plan 11-01 (per D-03). Keep current auth redirect logic untouched (D-05). On denied route, call feedback copy from UI-SPEC (“Access denied for this module. Redirecting to dashboard.”) and `router.replace("/dashboard")` (D-04), with loop-safe guard (avoid repeatedly redirecting when already on `/dashboard`).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- app/dashboard/layout.test.tsx</automated>
  </verify>
  <done>Direct URL bypass is closed and unauthorized routes are safely redirected with user feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add auth-flow regression tests for login redirect and logout return</name>
  <files>v0-ai-asset-management/app/page.test.tsx, v0-ai-asset-management/components/sidebar-auth.test.tsx</files>
  <behavior>
    - Test 1: login submit still invokes dashboard navigation (`/dashboard`) per FNDN-03 (D-05).
    - Test 2: sidebar logout still invokes store logout and returns to `/` per FNDN-06 (D-05).
    - Test 3: role-aware sidebar behavior remains unchanged while using shared matrix (FNDN-04, D-05).
  </behavior>
  <action>Create focused regression tests for `app/page.tsx` and sidebar logout interaction to prove no behavior regression after route-guard changes (per D-05). Keep assertions role-specific and path-specific to phase scope only; do not implement deferred full audit module behavior.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- app/page.test.tsx components/sidebar-auth.test.tsx</automated>
  </verify>
  <done>FNDN-03 and FNDN-06 behaviors are preserved under new route-guard implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path -> dashboard layout guard | Untrusted direct URL must be authorized by role |
| Client role state -> protected route access | Role spoofing risk in client-only demo auth model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | E | `app/dashboard/layout.tsx` | mitigate | Enforce role-path authorization check on every dashboard route render (D-03, D-04). |
| T-11-05 | R | denied-route redirect flow | mitigate | Emit explicit access-denied feedback message on redirect to `/dashboard` (D-04). |
| T-11-06 | S | client-only auth role source | accept | v1.1 is mock-auth UI demo; mitigation limited to consistent client-side guard + role-filtered nav until backend auth phase. |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npm test -- app/dashboard/layout.test.tsx app/page.test.tsx components/sidebar-auth.test.tsx`
- `cd v0-ai-asset-management && npm run build`
</verification>

<success_criteria>
- FNDN-03, FNDN-04, FNDN-06 fully satisfied for Phase 11 closure.
- Unauthorized direct URL navigation cannot access restricted modules.
- Login/logout and role-aware navigation remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-access-control-gap-closure/11-02-SUMMARY.md`
</output>
