---
phase: 11-navigation-access-control-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - v0-ai-asset-management/lib/navigation-access.ts
  - v0-ai-asset-management/lib/navigation-access.test.ts
  - v0-ai-asset-management/components/sidebar.tsx
  - v0-ai-asset-management/components/sidebar.test.tsx
  - v0-ai-asset-management/app/dashboard/audit/page.tsx
autonomous: true
requirements: [FNDN-04]
must_haves:
  truths:
    - "Visible sidebar links resolve to existing dashboard routes; Audit Log no longer points to a missing page."
    - "Admin and Auditor can open /dashboard/audit without 404."
    - "Audit page is explicitly read-only placeholder copy for Phase 10."
  artifacts:
    - path: "v0-ai-asset-management/lib/navigation-access.ts"
      provides: "Single-source route access matrix and route visibility helpers"
    - path: "v0-ai-asset-management/components/sidebar.tsx"
      provides: "Sidebar rendering from shared matrix (no duplicated role policy)"
    - path: "v0-ai-asset-management/app/dashboard/audit/page.tsx"
      provides: "Temporary read-only audit placeholder route"
  key_links:
    - from: "components/sidebar.tsx"
      to: "lib/navigation-access.ts"
      via: "visible navigation derived from shared access helpers"
      pattern: "getVisibleNavigation"
    - from: "sidebar Audit Log item"
      to: "/dashboard/audit"
      via: "Link href"
      pattern: "/dashboard/audit"
---

<objective>
Create a single source of truth for dashboard route access, wire Sidebar to it, and add the temporary Audit route placeholder.

Purpose: Close broken navigation gap while matching existing role semantics.
Output: Shared access matrix helper, Sidebar integration, and `/dashboard/audit` placeholder page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-navigation-access-control-gap-closure/11-CONTEXT.md
@.planning/phases/11-navigation-access-control-gap-closure/11-RESEARCH.md
@.planning/phases/11-navigation-access-control-gap-closure/11-UI-SPEC.md
@v0-ai-asset-management/components/sidebar.tsx
@v0-ai-asset-management/lib/data.ts

<interfaces>
From `lib/data.ts`:
```ts
export type UserRole = "Admin" | "Asset Manager" | "Staff" | "Auditor"
```

From `components/sidebar.tsx`:
```ts
type NavItem = {
  href: string
  label: string
  icon: React.ElementType
  roles: UserRole[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared dashboard route-access contract with tests</name>
  <files>v0-ai-asset-management/lib/navigation-access.ts, v0-ai-asset-management/lib/navigation-access.test.ts</files>
  <behavior>
    - Test 1: role-to-route matrix matches current sidebar semantics for all roles (D-03).
    - Test 2: `/dashboard/audit` is known route and allowed only for Admin/Auditor (D-01, D-03).
    - Test 3: helper returns role-filtered visible navigation labels including Audit Log only where allowed (D-03).
  </behavior>
  <action>Create `navigation-access.ts` exporting typed route policy primitives (`DASHBOARD_NAV`, `getVisibleNavigation(role)`, `canAccessDashboardRoute(role, pathname)`, `isKnownDashboardRoute(pathname)`) as the single policy source (per D-03). Keep Audit Log visibility consistent with current role policy and include `/dashboard/audit` in matrix per D-01.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- lib/navigation-access.test.ts</automated>
  </verify>
  <done>Shared route policy exists, is typed, and tests lock matrix semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Sidebar to shared policy and add audit placeholder route</name>
  <files>v0-ai-asset-management/components/sidebar.tsx, v0-ai-asset-management/components/sidebar.test.tsx, v0-ai-asset-management/app/dashboard/audit/page.tsx</files>
  <behavior>
    - Test 1: Sidebar renders route list from shared helper and keeps role-specific visibility (D-03, D-05).
    - Test 2: Audit placeholder page renders locked heading/body/action copy and no mutable controls (D-02).
    - Test 3: Audit nav item no longer leads to missing route for allowed roles (D-01).
  </behavior>
  <action>Replace local `NAV` matrix in Sidebar with imports from `lib/navigation-access.ts` (per D-03), keeping logout interaction unchanged (D-05). Add `/dashboard/audit/page.tsx` placeholder using UI-SPEC copy (“Audit Log module is scheduled for Phase 10”) and read-only informational layout only (per D-02, deferred full audit module remains out-of-scope).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- components/sidebar.test.tsx && npm run build</automated>
  </verify>
  <done>Audit link resolves to real route, placeholder is read-only, and sidebar policy comes from shared access helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar UI -> route tree | User clicks untrusted navigation paths that must resolve to valid pages |
| Shared policy -> route consumers | Policy drift risk if sidebar and guards diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | `components/sidebar.tsx` | mitigate | Remove duplicated local matrix and consume single shared helper (D-03). |
| T-11-02 | D | `/dashboard/audit` route | mitigate | Add concrete placeholder page so visible nav cannot hit 404 (D-01). |
| T-11-03 | I | `app/dashboard/audit/page.tsx` | mitigate | Keep page read-only with explicit Phase 10 messaging; no fake mutable controls (D-02). |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npm test -- lib/navigation-access.test.ts components/sidebar.test.tsx`
- `cd v0-ai-asset-management && npm run build`
</verification>

<success_criteria>
- FNDN-04 gap closure: sidebar links align to real routes and role visibility remains architecture-correct.
- Audit Log route exists and is non-404 for authorized roles.
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-access-control-gap-closure/11-01-SUMMARY.md`
</output>
