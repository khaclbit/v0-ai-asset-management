---
phase: 15-information-architecture-user-flows-navigation
plan: 01
type: execute
wave: 1
status: pending
depends_on: []
files_modified:
  - .planning/phases/15-information-architecture-user-flows-navigation/IA.md
autonomous: true
requirements: [IA-01, IA-02, IA-03]
nyquist_validation: true

must_haves:
  truths:
    - "Navigation map table lists all 11 modules with explicit ✅/👁️/🔒 per column for Administrator, Manager, and Staff"
    - "Sitemap route tree covers all 11 top-level modules plus sub-pages (≥ 20 routes total) with per-role access annotations and redirect targets"
    - "All 5 user flow diagrams are present as Mermaid flowchart TD blocks, each with a header, decision-node table, and traceability citation to Phase 14 SDD"
    - "AI mutation prohibition is explicit in Flow 3: AI never creates maintenance directly — Manager Approve is the only trigger"
    - "Flow Traceability Table at end of §3 maps all 5 flows to Phase 14 SDD sections and lists decision-node counts"
  artifacts:
    - path: ".planning/phases/15-information-architecture-user-flows-navigation/IA.md"
      provides: "Information Architecture document: §1 Navigation Map, §2 Sitemap, §3 User Flows"
      contains: "IA-01, IA-02, IA-03"
  key_links:
    - from: "§3 Flow 3"
      to: "Phase 14 SDD §2.3 + §2.4"
      via: "Traceability comment // Traces: SDD §2.3 and §2.4"
      pattern: "Traces: SDD"
    - from: "§2 Route table"
      to: "/login, /403"
      via: "Access Denied Redirect column"
      pattern: "dashboard.*login|dashboard.*403"
---

# Phase 15 — Plan 01: Information Architecture, User Flows & Navigation

## Goal

Produce a single `IA.md` document containing: (1) a complete navigation map with shell layout and role-based visibility rules for all 11 modules, (2) a full sitemap with route hierarchy and per-role access control annotations covering ≥ 20 routes, and (3) all 5 core user flow diagrams as Mermaid `flowchart TD` blocks, each traceable to Phase 14 SDD state machines.

## Output Artifact

Single file: `.planning/phases/15-information-architecture-user-flows-navigation/IA.md`

---

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-system-architecture-domain-model/SDD.md
</context>

---

## Tasks

<tasks>

<!-- ══════════════════════════════════════════════════════════════════ -->
<!-- TASK 1 — Navigation Map & Role Visibility Rules (IA-01)           -->
<!-- ══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1: Navigation Map & Role Visibility Rules (IA-01)</name>
  <files>.planning/phases/15-information-architecture-user-flows-navigation/IA.md</files>

  <action>
    Create the file IA.md with a YAML front-matter block, then write §1 Navigation Map.

    The front-matter must declare: phase 15, document IA, status final, date 2026-06-28, requirements IA-01 IA-02 IA-03.

    §1 Navigation Map contains four sub-sections:

    **1.1 — Shell Layout Diagram**
    Write a plain-text ASCII diagram (inside a code block labelled "text") showing the two-panel dashboard shell: a persistent left sidebar and a top bar. The layout must show these labelled zones: TOP BAR with [Breadcrumb] on the left and [Bell🔔 N] [Avatar ▼] Role on the right; SIDEBAR with [Logo] at top, primary nav items in the middle, a divider labelled "── Administration ──", admin-only items below it, and Settings + Logout at the bottom; PAGE CONTENT AREA filling the right panel. This matches the enterprise SaaS pattern (Grafana/Jira/Azure Portal) already present in the existing frontend prototype.

    **1.2 — Navigation Items Table**
    Write a Markdown table with columns: # | Nav Label | Route | Administrator | Manager | Staff | Section | Notes.
    Include all 11 modules in this exact order, using these symbols: ✅ = full access, 👁️ = filtered/read-only access, 🔒 = hidden (not shown in sidebar). The Notes column must explain filtering or constraints where applicable.

    Use these exact values per row:
    1 | Dashboard | /dashboard | ✅ | ✅ | ✅ | Primary | Default route after login for all roles
    2 | Assets | /dashboard/assets | ✅ | ✅ | 👁️ | Primary | Staff: filtered server-side by assignee_id — no dedicated My Assets route
    3 | Assignments | /dashboard/assignments | ✅ | ✅ | 👁️ | Primary | Staff: own requests only
    4 | Maintenance | /dashboard/maintenance | ✅ | ✅ | 🔒 | Primary | Staff has no sidebar access; read-only data surfaced via asset detail only
    5 | IoT Monitoring | /dashboard/iot | ✅ | ✅ | 🔒 | Primary | Staff has no sidebar access to IoT dashboard
    6 | AI Predictive | /dashboard/ai | ✅ | ✅ | 🔒 | Primary | Approve/Defer actions are Manager and Administrator only
    7 | Notifications | /dashboard/notifications | ✅ | ✅ | ✅ | Primary | Bell badge driven by SSE unread count; dropdown shows last 5 + "See all" link
    8 | Reports | /dashboard/reports | ✅ | ✅ | 🔒 | Primary | Single hub with Asset / Assignment / Maintenance tabs
    9 | Audit Log | /dashboard/audit | ✅ | 🔒 | 🔒 | Administration | Administrator only; consolidated from v1.1 Auditor role
    10 | User Management | /dashboard/users | ✅ | 🔒 | 🔒 | Administration | Administrator only
    11 | Settings | /dashboard/settings | ✅ | ✅ | ✅ | Bottom | All roles see profile settings; system config tab is Administrator only

    **1.3 — Role Visibility Summary**
    Write three bullet-point summaries, one per role, titled "Staff sees:", "Manager sees:", "Administrator sees:". Each lists the exact nav items visible to that role (derived from the table above). Add a highlighted callout box (blockquote) stating the hide-not-disable rule: role-restricted items are hidden entirely from the sidebar — Staff must never see a locked/greyed Audit Log entry.

    **1.4 — Sidebar & Top Bar Behaviour**
    Write a sub-section with four numbered behaviour rules:
    (a) Sidebar expanded state: icon + label, active item highlighted with background colour, width ~240 px.
    (b) Sidebar collapsed state (mobile md: breakpoint): icon-only rail ~48 px wide, using hidden md:flex pattern from existing prototype.
    (c) Active state matching: exact match pathname === item.href for top-level routes; prefix match pathname.startsWith(item.href + '/') for nested routes.
    (d) Notification badge: Notifications sidebar item shows an unread count badge driven by Zustand store unreadNotifications field updated via SSE push; badge disappears when count = 0.

    Write a sub-section for the top bar with three numbered items:
    (a) Left: page title + breadcrumb (e.g., Dashboard > Assets > ASSET-001 > Edit), computed from current route segments.
    (b) Right: notification bell icon with unread count badge — click opens /dashboard/notifications (full page).
    (c) Right: user avatar with name + role badge — click opens dropdown with two items: Settings (→ /dashboard/settings) and Logout.

    **1.5 — Sub-Navigation (In-Page Tabs and Nested Routes)**
    Write a Markdown table listing modules that have sub-pages or in-page tabs, with columns: Module | Sub-Page / Tab | Route | Available To.
    Rows:
    Assets | Asset List | /dashboard/assets | All roles (Staff: filtered)
    Assets | Asset Detail | /dashboard/assets/[id] | All roles (Staff: own only)
    Assets | Create Asset | /dashboard/assets/new | Administrator, Manager
    Assets | Edit Asset | /dashboard/assets/[id]/edit | Administrator, Manager
    Assignments | All Assignments | /dashboard/assignments | All roles (Staff: own)
    Assignments | Pending Queue | /dashboard/assignments (queue tab) | Administrator, Manager
    Assignments | Create Request | /dashboard/assignments/new | All roles
    Assignments | Assignment Detail | /dashboard/assignments/[id] | All roles (Staff: own)
    Assignments | Return Flow | /dashboard/assignments/[id]/return | All roles (Staff: own)
    Maintenance | Schedule View | /dashboard/maintenance | Administrator, Manager
    Maintenance | Create Ticket | /dashboard/maintenance/new | Administrator, Manager
    Maintenance | Ticket Detail | /dashboard/maintenance/[id] | Administrator, Manager
    Maintenance | Update State | /dashboard/maintenance/[id]/edit | Administrator, Manager
    IoT | Hub (asset selector) | /dashboard/iot | Administrator, Manager
    IoT | Asset Sensor Detail | /dashboard/iot/[assetId] | Administrator, Manager
    AI Predictive | Recommendations List | /dashboard/ai | Administrator, Manager
    AI Predictive | Recommendation Detail | /dashboard/ai/[recommendationId] | Administrator, Manager
    Reports | Asset Report tab | /dashboard/reports (tab) | Administrator, Manager
    Reports | Assignment Report tab | /dashboard/reports (tab) | Administrator, Manager
    Reports | Maintenance Report tab | /dashboard/reports (tab) | Administrator, Manager
    Settings | User Profile | /dashboard/settings/profile | All roles
    Settings | System Config | /dashboard/settings/system | Administrator only
    Users | User List | /dashboard/users | Administrator
    Users | Create User | /dashboard/users/new | Administrator
    Users | Edit User | /dashboard/users/[id]/edit | Administrator
  </action>

  <verify>
    <automated>grep -c "✅\|👁️\|🔒" .planning/phases/15-information-architecture-user-flows-navigation/IA.md</automated>
  </verify>
  <done>IA.md §1 Navigation Map is written. The navigation items table contains all 11 modules with ✅/👁️/🔒 per role column. The role visibility summaries and sidebar/topbar behaviour rules are present. grep -c "✅\|👁️\|🔒" IA.md returns ≥ 20.</done>
</task>

<!-- ══════════════════════════════════════════════════════════════════ -->
<!-- TASK 2 — Sitemap with Route Hierarchy & Access Control (IA-02)    -->
<!-- ══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 2: Sitemap — Route Hierarchy & Access Control (IA-02)</name>
  <files>.planning/phases/15-information-architecture-user-flows-navigation/IA.md</files>

  <action>
    Append §2 Sitemap to IA.md. This section contains three sub-sections.

    **2.1 — Route Hierarchy Tree**
    Write the full route tree as a plain-text ASCII tree inside a code block (labelled "text"). The tree must show all public routes and all authenticated routes nested under /dashboard. Use ├──, └──, and │ connectors. Include all of the following routes (with role annotations in square brackets after each route):

    / (root)
    ├── / ← Login page [Public — all]
    │   └── /login ← alias
    └── /dashboard ← Authenticated shell (JWT required)
        ├── /dashboard ← Dashboard Overview [All roles]
        ├── /dashboard/assets ← Asset List [All roles; Staff: filtered to own]
        │   ├── /dashboard/assets/new ← Create Asset [Administrator, Manager]
        │   └── /dashboard/assets/[id] ← Asset Detail [All roles; Staff: own only]
        │       └── /dashboard/assets/[id]/edit ← Edit Asset [Administrator, Manager]
        ├── /dashboard/assignments ← Assignment List [All roles; Staff: own requests]
        │   ├── /dashboard/assignments/new ← Create Request [All roles]
        │   └── /dashboard/assignments/[id] ← Assignment Detail [All roles; Staff: own]
        │       └── /dashboard/assignments/[id]/return ← Return Flow [All roles; Staff: own]
        ├── /dashboard/maintenance ← Maintenance Schedule [Administrator, Manager]
        │   ├── /dashboard/maintenance/new ← Create Ticket [Administrator, Manager]
        │   └── /dashboard/maintenance/[id] ← Ticket Detail [Administrator, Manager]
        │       └── /dashboard/maintenance/[id]/edit ← Update State [Administrator, Manager]
        ├── /dashboard/iot ← IoT Monitoring Hub [Administrator, Manager]
        │   └── /dashboard/iot/[assetId] ← Asset Sensor Detail [Administrator, Manager]
        ├── /dashboard/ai ← AI Predictive Recommendations [Administrator, Manager]
        │   └── /dashboard/ai/[recommendationId] ← Recommendation Detail [Administrator, Manager]
        ├── /dashboard/notifications ← Notification Center [All roles]
        ├── /dashboard/reports ← Reports Hub [Administrator, Manager]
        │   ├── /dashboard/reports/assets ← Asset Report [Administrator, Manager]
        │   ├── /dashboard/reports/assignments ← Assignment Report [Administrator, Manager]
        │   └── /dashboard/reports/maintenance ← Maintenance Report [Administrator, Manager]
        ├── /dashboard/audit ← Audit Log [Administrator only]
        ├── /dashboard/users ← User Management [Administrator only]
        │   ├── /dashboard/users/new ← Create User [Administrator only]
        │   └── /dashboard/users/[id]/edit ← Edit User [Administrator only]
        └── /dashboard/settings ← Settings / Profile [All roles]
            ├── /dashboard/settings/profile ← User Profile [All roles]
            └── /dashboard/settings/system ← System Config [Administrator only]

    Also include: /403 ← Access Denied page [rendered when role check fails] and /login ← redirected to when unauthenticated.

    **2.2 — Mermaid Route Graph**
    Write a Mermaid graph TD diagram that shows the parent→child relationships between all routes. Node labels should be the route path in short form (omit /dashboard prefix for child nodes for readability). Include all top-level and second-level nodes. Use subgraph blocks to group: subgraph AUTH ["Public / Auth"], subgraph DASH ["Dashboard Shell"], and a subgraph ADMIN ["Admin-Only"] for /dashboard/audit and /dashboard/users. Nodes for routes that require Administrator-only access should have a note in the label (e.g., Audit["audit 🔐Admin"]).

    The graph should include the following nodes and edges:
    - Root → Login, Root → Dashboard
    - Dashboard → Assets, Assignments, Maintenance, IoT, AI, Notifications, Reports, Audit, Users, Settings
    - Assets → AssetsNew["assets/new"], AssetDetail["assets/[id]"]
    - AssetDetail → AssetEdit["assets/[id]/edit"]
    - Assignments → AssignNew["assignments/new"], AssignDetail["assignments/[id]"]
    - AssignDetail → AssignReturn["assignments/[id]/return"]
    - Maintenance → MaintNew["maintenance/new"], MaintDetail["maintenance/[id]"]
    - MaintDetail → MaintEdit["maintenance/[id]/edit"]
    - IoT → IoTAsset["iot/[assetId]"]
    - AI --> AIDetail["ai/[recId]"]
    - Reports → ReportsAssets["reports/assets"], ReportsAssign["reports/assignments"], ReportsMaint["reports/maintenance"]
    - Users → UsersNew["users/new"], UsersEdit["users/[id]/edit"]
    - Settings → SettingsProfile["settings/profile"], SettingsSystem["settings/system 🔐Admin"]
    - Root → Unauthorized["/403"]

    **2.3 — Route-Level Access Control Table**
    Write a Markdown table with columns: Route Pattern | Administrator | Manager | Staff | Redirect if Denied.
    Include all routes listed in the tree. Use ✅ for access, ✅ (own) for filtered access, → /403 for role-denied, and → /login for unauthenticated. Provide at least 22 rows covering all routes.

    Exact rows to include:
    /login | ✅ (redirect to /dashboard) | ✅ | ✅ | —
    /dashboard | ✅ | ✅ | ✅ | → /login (if unauthenticated)
    /dashboard/assets | ✅ | ✅ | ✅ (own) | → /login
    /dashboard/assets/new | ✅ | ✅ | → /403 | /403
    /dashboard/assets/[id] | ✅ | ✅ | ✅ (own only) | /403
    /dashboard/assets/[id]/edit | ✅ | ✅ | → /403 | /403
    /dashboard/assignments | ✅ | ✅ | ✅ (own) | → /login
    /dashboard/assignments/new | ✅ | ✅ | ✅ | —
    /dashboard/assignments/[id] | ✅ | ✅ | ✅ (own) | /403
    /dashboard/assignments/[id]/return | ✅ | ✅ | ✅ (own) | /403
    /dashboard/maintenance | ✅ | ✅ | → /403 | /403
    /dashboard/maintenance/new | ✅ | ✅ | → /403 | /403
    /dashboard/maintenance/[id] | ✅ | ✅ | → /403 | /403
    /dashboard/maintenance/[id]/edit | ✅ | ✅ | → /403 | /403
    /dashboard/iot | ✅ | ✅ | → /403 | /403
    /dashboard/iot/[assetId] | ✅ | ✅ | → /403 | /403
    /dashboard/ai | ✅ | ✅ | → /403 | /403
    /dashboard/ai/[recommendationId] | ✅ | ✅ | → /403 | /403
    /dashboard/notifications | ✅ | ✅ | ✅ | → /login
    /dashboard/reports | ✅ | ✅ | → /403 | /403
    /dashboard/reports/assets | ✅ | ✅ | → /403 | /403
    /dashboard/reports/assignments | ✅ | ✅ | → /403 | /403
    /dashboard/reports/maintenance | ✅ | ✅ | → /403 | /403
    /dashboard/audit | ✅ | → /403 | → /403 | /403
    /dashboard/users | ✅ | → /403 | → /403 | /403
    /dashboard/users/new | ✅ | → /403 | → /403 | /403
    /dashboard/users/[id]/edit | ✅ | → /403 | → /403 | /403
    /dashboard/settings | ✅ | ✅ | ✅ | → /login
    /dashboard/settings/profile | ✅ | ✅ | ✅ | → /login
    /dashboard/settings/system | ✅ | → /403 | → /403 | /403
    /403 | ✅ | ✅ | ✅ | — (public error page)

    After the table, add a callout (blockquote) describing the two-layer enforcement model:
    Layer 1 (UX): sidebar hides links for inaccessible routes via getVisibleNavigation(role) — role-restricted items never appear in the sidebar.
    Layer 2 (Security): backend FastAPI returns HTTP 403 on every protected endpoint regardless of UI state — direct URL access or API calls are enforced server-side. Frontend-only RBAC is insufficient and must never be the sole guard.
  </action>

  <verify>
    <automated>grep -c "dashboard\|/login\|/403\|/unauthorized" .planning/phases/15-information-architecture-user-flows-navigation/IA.md</automated>
  </verify>
  <done>IA.md §2 Sitemap is written. The route tree covers all 11 top-level modules plus sub-pages (≥ 20 routes). The Mermaid graph TD shows parent→child relationships. The access control table has ≥ 30 rows with Administrator/Manager/Staff columns and redirect targets. grep -c "dashboard\|/login\|/403" IA.md returns ≥ 20.</done>
</task>

<!-- ══════════════════════════════════════════════════════════════════ -->
<!-- TASK 3 — 5 User Flow Diagrams (IA-03)                             -->
<!-- ══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 3: Five User Flow Diagrams (IA-03)</name>
  <files>.planning/phases/15-information-architecture-user-flows-navigation/IA.md</files>

  <action>
    Append §3 User Flows to IA.md. Write a short preamble explaining the Mermaid flowchart TD conventions used: rounded rectangles ([( )]) for Start/End terminals, rectangles ([ ]) for user actions or system operations, diamonds ({ }) for decision nodes, role annotations inline (👤 Staff, 👤 Manager, 👤 Admin, 🤖 System), and arrow labels (e.g., -->|"Approved"|) for transitions. Then write all five flows.

    Each flow must follow this structure: (a) a Level-3 heading with the flow number and name, (b) a metadata block (plain text or table) listing Entry Point, Actors, and Phase 14 SDD Traceability, (c) the Mermaid flowchart TD code block, (d) a Key Decision Nodes table with columns Decision | Guard Condition | Outcome, (e) a traceability comment line: "// Traces: SDD §X.X [section name]".

    ─────────────────────────────────────────────────────────────────────
    FLOW 1 — Asset Lifecycle Transitions
    ─────────────────────────────────────────────────────────────────────
    Entry: Administrator or Manager creates a new asset via /dashboard/assets/new.
    Actors: Administrator (create, retire), Manager (create, transition), System (state machine).
    Traceability: Phase 14 SDD §2.2 Asset Lifecycle State Machine.

    The Mermaid diagram must model these exact nodes and edges:
    Start([▶ Start: Admin/Manager / Adds New Asset])
    F1[👤 Admin/Manager / Fill Create Asset Form / /dashboard/assets/new]
    D1{Form valid? / All required fields?}
    F2[🤖 System / Create asset record / State: REGISTERED]
    D2{Asset physically / verified & ready?}
    F3[👤 Admin/Manager / Update state → AVAILABLE / /dashboard/assets/id/edit]
    D3{Assignment / request approved?}
    F4[🤖 System / Update state → ASSIGNED / (on assignment approval)]
    D4{Return validated / by Manager?}
    F5[🤖 System / Update state → AVAILABLE / (on return close)]
    D5{Maintenance / needed?}
    F6[🤖 System / Update state → MAINTENANCE / (on ticket creation)]
    D6{Maintenance / completed?}
    F7[🤖 System / Update state → AVAILABLE / (on ticket completion)]
    D7{Retire / decision?}
    F8[👤 Admin / Confirm retirement dialog / /dashboard/assets/id/edit]
    F9[🤖 System / Update state → RETIRED / Audit event logged]
    End([⏹ End: Asset Retired])

    Edges:
    Start --> F1
    F1 --> D1
    D1 -->|"Invalid"| F1
    D1 -->|"Valid"| F2
    F2 --> D2
    D2 -->|"No"| D2
    D2 -->|"Yes"| F3
    F3 --> D3
    D3 -->|"No"| F3
    D3 -->|"Approved"| F4
    F4 --> D4
    D4 -->|"No"| F4
    D4 -->|"Yes"| F5
    F5 --> D5
    D5 -->|"Yes"| F6
    D5 -->|"No"| D7
    F6 --> D6
    D6 -->|"No"| F6
    D6 -->|"Yes"| F7
    F7 --> D7
    D7 -->|"No"| F3
    D7 -->|"Yes"| F8
    F8 --> F9
    F9 --> End

    Key Decision Nodes table (6 rows):
    Form valid? | All required fields populated | Valid → REGISTERED; Invalid → re-prompt
    Asset verified? | Physical inspection complete | Yes → AVAILABLE; No → wait
    Assignment approved? | Manager/Admin approval action | Approved → ASSIGNED
    Return validated? | Manager validates return condition | Yes → AVAILABLE
    Maintenance needed? | Manual or AI-triggered decision | Yes → MAINTENANCE
    Retire decision? | Administrator-only action (confirmation dialog required) | Yes → RETIRED; No → continue lifecycle

    Traceability line: // Traces: SDD §2.2 Asset Lifecycle State Machine (Registered → Available → Assigned → Maintenance → Retired)

    ─────────────────────────────────────────────────────────────────────
    FLOW 2 — Assignment Request → Approval → Return
    ─────────────────────────────────────────────────────────────────────
    Entry: Staff (or Manager) submits an assignment request from /dashboard/assignments.
    Actors: Staff (submit request, initiate return), Manager (approve/reject, validate return), System (state transitions, notifications).
    Traceability: Phase 14 SDD §2.2 Asset Lifecycle (Assigned state) + §2.1 Manager permissions.

    The Mermaid diagram must model these exact nodes and edges:
    Start([▶ Start: Staff or Manager / Initiates Assignment Request])
    A1[👤 Staff/Manager / Fill Assignment Request Form / /dashboard/assignments/new / (asset, assignee, expected return date)]
    A2{Form / valid?}
    A3[🤖 System / Create assignment record / State: REQUESTED / Notify Manager]
    A4[👤 Manager / Review Pending Request / /dashboard/assignments (queue)]
    A5{Manager / Decision}
    A6[🤖 System / State → REJECTED / Notify requestor]
    A7[🤖 System / State → ACTIVE / Asset → ASSIGNED / Notify assignee]
    A8{Assignment / overdue?}
    A9[🤖 System / Mark OVERDUE badge / Notify Manager + assignee]
    A10[👤 Staff/Manager / Initiate Return / /dashboard/assignments/id/return]
    A11[👤 Manager / Validate return condition / /dashboard/assignments/id]
    A12{Asset / condition OK?}
    A13[🤖 System / State → CLOSED / Asset → AVAILABLE / Audit event logged]
    A14[🤖 System / Flag damage noted / State → CLOSED (w/ note) / Trigger maintenance if needed]
    EndR([⏹ End: Assignment Closed])
    EndRej([⏹ End: Request Rejected])

    Edges:
    Start --> A1
    A1 --> A2
    A2 -->|"Invalid"| A1
    A2 -->|"Valid"| A3
    A3 --> A4
    A4 --> A5
    A5 -->|"Reject"| A6
    A6 --> EndRej
    A5 -->|"Approve"| A7
    A7 --> A8
    A8 -->|"Yes"| A9
    A9 --> A10
    A8 -->|"No"| A10
    A10 --> A11
    A11 --> A12
    A12 -->|"OK"| A13
    A12 -->|"Damaged"| A14
    A13 --> EndR
    A14 --> EndR

    Key Decision Nodes table (4 rows):
    Form valid? | Asset available + dates valid | Valid → REQUESTED queue entry
    Manager decision | Approve / Reject | ACTIVE (asset → ASSIGNED) or REJECTED
    Overdue? | Expected return date has passed | Yes → OVERDUE badge + notification to Manager
    Asset condition OK? | Manager inspection on return | OK → CLOSED; Damaged → CLOSED with damage note + optional maintenance trigger

    Traceability line: // Traces: SDD §2.2 Assignment Lifecycle (Requested → Active → Overdue / Closed / Rejected) + §2.1 Manager role permissions

    ─────────────────────────────────────────────────────────────────────
    FLOW 3 — Maintenance Ticket Creation via AI Recommendation
    ─────────────────────────────────────────────────────────────────────
    Entry: AI Predictive ML worker generates a high-risk recommendation (auto-triggered by sensor data analysis).
    Actors: AI/System (generate recommendation), Manager (review and approve or defer), System (auto-create maintenance ticket on approval only).
    Traceability: Phase 14 SDD §2.3 Maintenance Lifecycle + §2.4 AI Recommendation Lifecycle + §1.4 AI Predictive Pipeline.

    CRITICAL CONSTRAINT (must be visible in the diagram and in a bold callout before the Mermaid block):
    The AI system NEVER creates a maintenance ticket directly. The Manager's Approve action is the sole trigger for maintenance creation. This enforces the AI mutation prohibition from Phase 14 SDD §1.2.

    The Mermaid diagram must model these exact nodes and edges:
    Start([▶ Start: Predictive ML Worker / Generates High-Risk Recommendation])
    M1[🤖 AI System / Analyze sensor_readings / Compute risk score & confidence / Create ai_recommendation record / State: PENDING]
    M2[🤖 System / Notify Manager via SSE / Show badge on AI Predictive nav item]
    M3[👤 Manager / View Recommendation Card / /dashboard/ai / See: risk band, confidence, contributing factors, SLA countdown]
    M4{SLA / deadline passed?}
    M5[🤖 System / Escalate: notify Admin / Mark as SLA_BREACHED]
    M6{Manager / Action}
    M7[🤖 System / State → DEFERRED / SLA countdown resets / Audit event logged]
    M8[🤖 System / State → APPROVED / Auto-create MaintenanceRecord / State: SCHEDULED / Asset → MAINTENANCE / Audit event logged]
    M9[👤 Manager / View created Maintenance Ticket / /dashboard/maintenance/id]
    End([⏹ End: Maintenance Ticket / Scheduled & Assigned])
    EndDefer([⏹ End: Recommendation Deferred / (revisit in next cycle)])

    Edges:
    Start --> M1
    M1 --> M2
    M2 --> M3
    M3 --> M4
    M4 -->|"Yes"| M5
    M5 --> M6
    M4 -->|"No"| M6
    M6 -->|"Defer"| M7
    M7 --> EndDefer
    M6 -->|"Approve"| M8
    M8 --> M9
    M9 --> End

    Key Decision Nodes table (2 rows):
    SLA deadline passed? | Current timestamp > recommendation.sla_deadline | Yes → escalate to Admin (SLA_BREACHED); No → Manager decides
    Manager action | Approve / Defer | Approve → System auto-creates MaintenanceRecord (SCHEDULED); Defer → DEFERRED state, SLA resets

    Traceability line: // Traces: SDD §2.3 Maintenance Lifecycle (Scheduled state creation) + §2.4 AI Recommendation Lifecycle (Pending → Approved → triggers maintenance / Deferred → Expired) + §1.4 AI Predictive Pipeline

    ─────────────────────────────────────────────────────────────────────
    FLOW 4 — AI Recommendation Approval by Manager (Dedicated Review)
    ─────────────────────────────────────────────────────────────────────
    Entry: Manager opens /dashboard/ai to review the pending recommendations queue.
    Actors: Manager (primary reviewer), Administrator (escalation path for SLA breach).
    Traceability: Phase 14 SDD §2.4 AI Recommendation Lifecycle.

    The Mermaid diagram must model these exact nodes and edges:
    Start([▶ Start: Manager / Navigates to /dashboard/ai])
    R1[👤 Manager / View Recommendation List / (filtered: PENDING, sorted by risk desc)]
    R2{Any PENDING / recommendations?}
    R3[UI: Empty state / 'No pending recommendations']
    R4[👤 Manager / Open Recommendation Detail / /dashboard/ai/id / View: asset, sensor data, risk score, contributing factors, SLA countdown, correlation_id]
    R5{Risk / Band?}
    R6[View: HIGH risk card / Red badge, SLA countdown visible / Approve / Defer buttons active]
    R7[View: MEDIUM risk card / Yellow badge, Approve / Defer active]
    R8[View: LOW risk card / Green badge, Defer recommended]
    R9{Manager / Action}
    R10[👤 Manager clicks APPROVE / Confirmation dialog shown]
    R11{Confirm / approval?}
    R12[🤖 System / State → APPROVED / Auto-creates MaintenanceRecord (SCHEDULED) / Asset state → MAINTENANCE / Broadcast notification / Audit event: manager_id, recommendation_id, timestamp]
    R13[👤 Manager clicks DEFER / Optional reason text]
    R14[🤖 System / State → DEFERRED / SLA countdown extended / Audit event logged]
    R15[🤖 System / Return to recommendation list / Show success toast]
    EndA([⏹ End: Approved — / Maintenance ticket created])
    EndD([⏹ End: Deferred — / Revisit on next AI cycle])
    EndE([⏹ End: No action — / Queue empty])

    Edges:
    Start --> R1
    R1 --> R2
    R2 -->|"No"| R3
    R3 --> EndE
    R2 -->|"Yes"| R4
    R4 --> R5
    R5 -->|"HIGH"| R6
    R5 -->|"MEDIUM"| R7
    R5 -->|"LOW"| R8
    R6 --> R9
    R7 --> R9
    R8 --> R9
    R9 -->|"Approve"| R10
    R10 --> R11
    R11 -->|"Cancel"| R4
    R11 -->|"Confirm"| R12
    R12 --> R15
    R15 --> EndA
    R9 -->|"Defer"| R13
    R13 --> R14
    R14 --> R15
    R15 --> EndD

    Key Decision Nodes table (3 rows):
    Any PENDING recommendations? | Queue count > 0 | Yes → open detail; No → empty state
    Risk band? | HIGH (>80%) / MEDIUM (40–80%) / LOW (<40%) | Determines card visual treatment (red/yellow/green badge); all bands support Approve/Defer
    Confirm approval? | Manager confirms in dialog | Confirm → maintenance auto-created; Cancel → returns to detail view (prevents accidental approval)

    Add a design note after the table: Confirmation dialog is mandatory before approval because the action is irreversible — it auto-creates a maintenance ticket and changes asset state. All approval and deferral actions are audit-logged with manager_id, recommendation_id, and timestamp per Phase 14 SDD §1.4 audit requirements.

    Traceability line: // Traces: SDD §2.4 AI Recommendation Lifecycle (Pending → Approved / Deferred → Expired) + REQUIREMENTS.md PRED-03 (manager approve/defer UI)

    ─────────────────────────────────────────────────────────────────────
    FLOW 5 — IoT Sensor Alert Response Workflow
    ─────────────────────────────────────────────────────────────────────
    Entry: A sensor reading published by the Python Simulator via MQTT exceeds the configured threshold.
    Actors: IoT Ingestion Module/System (detection, notification creation), Manager/Administrator (review and response).
    Traceability: Phase 14 SDD §1.3 IoT Data Pipeline + §1.5 Notification Pipeline + §2.6 Sensor Category Mapping.

    The Mermaid diagram must model these exact nodes and edges:
    Start([▶ Start: Python Sensor Simulator / Publishes reading via MQTT])
    S1[🤖 IoT Ingestion Module / MQTT subscriber receives message / Topic: assets/{id}/sensors/{type} / Dedup via message_id / INSERT into sensor_readings]
    S2[🤖 Backend / WebSocket broadcaster / Push live reading to connected clients / (5s aggregation window)]
    S3[🤖 Threshold Checker / (background task) / Compare value vs threshold config / per sensor_type + asset_category]
    S4{Threshold / exceeded?}
    S5[Normal flow: value stored / No alert generated]
    S6[🤖 Notification Hub / Create notification record / Type: IOT_THRESHOLD_BREACH / Severity: based on delta above threshold / Notify Manager + Admin via SSE]
    S7[👤 Manager/Admin / See bell badge increment / Receive SSE push notification / Alert toast in UI]
    S8[👤 Manager/Admin / Navigate to IoT Dashboard / /dashboard/iot / View asset selector sidebar / Select alerted asset]
    S9[👤 Manager/Admin / View sensor tile grid / /dashboard/iot/assetId / See: current value (red), threshold line, time-series chart, last-updated timestamp]
    S10{Severity / Assessment}
    S11[👤 Manager / Acknowledge alert / (mark notification as read) / No maintenance needed]
    S12[👤 Manager / Create Maintenance Ticket / /dashboard/maintenance/new / (pre-filled: asset, sensor context) / State: SCHEDULED]
    S13[🤖 System / Audit event: alert_acknowledged / correlation_id: notification_id]
    S14[🤖 System / Asset → MAINTENANCE state / Audit event: maintenance_created_from_iot / correlation_id: notification_id]
    EndA([⏹ End: Alert Acknowledged / (normal variation, no action)])
    EndM([⏹ End: Maintenance Ticket Created / Sensor alert resolved via workflow])
    EndN([⏹ End: Normal reading stored / No alert pathway triggered])

    Edges:
    Start --> S1
    S1 --> S2
    S2 --> S3
    S3 --> S4
    S4 -->|"No"| S5
    S5 --> EndN
    S4 -->|"Yes"| S6
    S6 --> S7
    S7 --> S8
    S8 --> S9
    S9 --> S10
    S10 -->|"Minor / transient"| S11
    S11 --> S13
    S13 --> EndA
    S10 -->|"Persistent / severe"| S12
    S12 --> S14
    S14 --> EndM

    Key Decision Nodes table (2 rows):
    Threshold exceeded? | Sensor value > threshold config for that sensor_type + asset_category | Yes → IOT_THRESHOLD_BREACH notification; No → normal storage, no alert
    Severity assessment | Manager/Admin judgment based on time-series chart and recurrence | Minor/transient → acknowledge; Persistent/severe → create maintenance ticket

    Add a design note: The IoT Monitoring page uses an asset-selector sidebar on the left (default: first asset pre-selected) and a sensor tile grid on the right. Alerted assets are highlighted with a red border. The correlation_id field links the notification to the maintenance ticket (if created), enabling full end-to-end audit traceability per SDD §1.5. Staff users do NOT have access to the IoT dashboard — they are notified only via the general Notification Center if the alert affects their assigned asset.

    Traceability line: // Traces: SDD §1.3 IoT Data Pipeline (MQTT → FastAPI → PostgreSQL → WebSocket → React) + §1.5 Notification Pipeline + §2.6 Sensor Category Mapping

    ─────────────────────────────────────────────────────────────────────
    FLOW TRACEABILITY TABLE
    ─────────────────────────────────────────────────────────────────────
    After all 5 flows, write a Level-3 heading "Flow Traceability Summary" and a Markdown table with columns:
    Flow # | Flow Name | Entry Point | Roles Involved | Phase 14 SDD Reference | Decision Nodes Count

    Rows:
    1 | Asset Lifecycle Transitions | Admin/Manager at /dashboard/assets/new | Administrator, Manager | §2.2 Asset Lifecycle State Machine | 6
    2 | Assignment Request → Approval → Return | Staff/Manager at /dashboard/assignments/new | Staff, Manager, Administrator | §2.2 Assignment Lifecycle + §2.1 Role Permissions | 4
    3 | Maintenance via AI Recommendation | AI ML worker (background) | Manager, Administrator, AI System | §2.3 Maintenance Lifecycle + §2.4 AI Recommendation Lifecycle + §1.4 AI Pipeline | 2
    4 | AI Recommendation Approval | Manager at /dashboard/ai | Manager, Administrator | §2.4 AI Recommendation Lifecycle + PRED-03 | 3
    5 | IoT Sensor Alert Response | MQTT sensor reading exceeds threshold | Manager, Administrator, IoT System | §1.3 IoT Data Pipeline + §1.5 Notification Pipeline + §2.6 Sensor Categories | 2

    After the table, add a paragraph noting: "All flows in this document derive their state machines, role permissions, and system boundaries from Phase 14 SDD. The SDD is the authoritative source of truth; if a state transition in IA.md conflicts with SDD §2.x, the SDD takes precedence."
  </action>

  <verify>
    <automated>grep -c "flowchart\|Traces: SDD\|subgraph" .planning/phases/15-information-architecture-user-flows-navigation/IA.md</automated>
  </verify>
  <done>IA.md §3 User Flows is written. All 5 Mermaid flowchart TD diagrams are present. Each flow has a header (entry, actors, traceability), Mermaid block, decision-node table, and "// Traces: SDD" line. The Flow Traceability Summary table maps all 5 flows to Phase 14 SDD sections. grep -c "flowchart\|Traces: SDD" IA.md returns ≥ 10.</done>
</task>

</tasks>

---

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js App Router | Route guard (middleware.ts) prevents unauthenticated access to /dashboard/* |
| React Component → FastAPI | Every API call must include JWT; backend enforces role check independently of frontend sidebar visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | Frontend sidebar RBAC | mitigate | Sidebar hiding is UX-only. Backend must return HTTP 403 on every role-restricted API endpoint — documented explicitly in §2.3 two-layer enforcement model |
| T-15-02 | Information Disclosure | /403 error page | mitigate | Access denied page must NOT reveal which roles can access the denied resource (per §4.5 in RESEARCH.md) |
| T-15-03 | Elevation of Privilege | AI mutation path | mitigate | AI recommendation NEVER creates maintenance directly — Manager Approve is the sole trigger (documented as critical constraint in Flow 3 and enforced at API layer) |
| T-15-04 | Tampering | correlation_id links (IoT → Maintenance) | accept | correlation_id is a read-only audit field set on creation; no user action can modify it post-creation |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This is a documentation-only phase — no package installs occur |
</threat_model>

---

## Verification Commands

| Req | Command | Pass Condition |
|-----|---------|----------------|
| IA-01 | `grep -c "✅\|👁️\|🔒" .planning/phases/15-information-architecture-user-flows-navigation/IA.md` | ≥ 20 |
| IA-02 | `grep -c "dashboard\|/login\|/403" .planning/phases/15-information-architecture-user-flows-navigation/IA.md` | ≥ 20 |
| IA-03 | `grep -c "flowchart\|Traces: SDD" .planning/phases/15-information-architecture-user-flows-navigation/IA.md` | ≥ 10 |
| ALL | `grep -c "§1\|§2\|§3" .planning/phases/15-information-architecture-user-flows-navigation/IA.md` | ≥ 3 (all three sections present) |

---

<verification>
After all three tasks are complete, run the four verification commands above. If any command returns below the pass condition, identify the missing content and add it to IA.md before marking the phase complete.

Additionally perform these manual checks:
1. Flow 3 contains the bold AI mutation prohibition callout ("AI NEVER creates maintenance directly").
2. The navigation table in §1 has exactly 11 module rows.
3. The route table in §2 has ≥ 30 rows with all three role columns populated.
4. The Flow Traceability Summary table at the end of §3 has exactly 5 rows.
5. Every "// Traces: SDD §" line cites a section number (e.g., §2.2, §1.3).
</verification>

---

<success_criteria>
1. Navigation map in §1 shows all 11 modules with a per-role visibility column (Administrator / Manager / Staff) using ✅/👁️/🔒 notation, plus sidebar states, top bar items, and sub-navigation table.
2. Sitemap in §2 covers all 11 top-level modules and their sub-pages (≥ 20 routes total), presented as both an ASCII tree and a Mermaid graph TD, with a route-level access control table showing redirect targets for denied access.
3. All 5 user flow diagrams in §3 are Mermaid flowchart TD blocks, each with a header (entry point, actors, traceability), a key decision nodes table, and a "// Traces: SDD §X.X" traceability line.
4. Flow 3 explicitly states that AI never creates maintenance directly — the Manager Approve action is the sole trigger — and this constraint is traceable to Phase 14 SDD §1.2.
</success_criteria>

---

## Source Audit

| Source Type | Item | Covered By |
|-------------|------|------------|
| GOAL | Produce complete navigation map, sitemap, and 5 user flow diagrams grounded in Phase 14 actor/role model | Task 1 (nav map), Task 2 (sitemap), Task 3 (flows) |
| REQ | IA-01 Navigation map with role visibility rules | Task 1 |
| REQ | IA-02 Sitemap with route hierarchy and access control | Task 2 |
| REQ | IA-03 User flow diagrams for 5 core journeys | Task 3 |
| RESEARCH | §1 Navigation architecture patterns + sidebar items table | Task 1 — all 11 modules inlined in action |
| RESEARCH | §2 Full sitemap tree with role annotations | Task 2 — full tree inlined in action |
| RESEARCH | §3 All 5 user flow Mermaid diagrams | Task 3 — all 5 diagrams inlined in action |
| RESEARCH | §4 RBAC implementation two-layer model | Task 2 §2.3 callout |
| RESEARCH | §5 All 6 open questions (all RESOLVED) | Reflected in decisions: no /my-assets route, single /reports hub, /settings dedicated route, IoT default sidebar, bell dropdown |
| CONTEXT | Staff assets: single /dashboard/assets filtered server-side | Task 1 nav table Notes column + Task 2 route table |
| CONTEXT | Reports: single /dashboard/reports hub with tabs | Task 1 §1.5 sub-nav table + Task 2 route tree |
| CONTEXT | Settings: /dashboard/settings route + avatar dropdown | Task 1 §1.4 top bar items + Task 2 route tree |
| CONTEXT | IoT default: asset-selector sidebar, first asset pre-selected | Task 3 Flow 5 design note |
| CONTEXT | Notifications: bell dropdown (last 5) + /dashboard/notifications full page | Task 1 §1.4 notification badge + nav table Notes |

All RESEARCH.md and CONTEXT.md items are covered. No deferred ideas are present. No items are missing.

---

<output>
After completing all tasks, create the plan summary at:
`.planning/phases/15-information-architecture-user-flows-navigation/15-01-SUMMARY.md`

The summary must record:
- Which sections were written in IA.md (§1, §2, §3)
- The verification command results (grep counts)
- Any deviations from the plan (none expected)
- The output artifact path: `.planning/phases/15-information-architecture-user-flows-navigation/IA.md`
</output>
