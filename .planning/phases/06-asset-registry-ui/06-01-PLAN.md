---
phase: 06-asset-registry-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - v0-ai-asset-management/app/dashboard/assets/page.tsx
autonomous: true
requirements:
  - ASSET-01
  - ASSET-05
  - ASSET-06
must_haves:
  truths:
    - "User can view assets in a paginated table with lifecycle badges."
    - "User can filter by category and lifecycle status at the same time."
    - "User can search by name, asset ID, and serial with debounce."
    - "User can reset all filters/search with one click."
  artifacts:
    - path: "v0-ai-asset-management/app/dashboard/assets/page.tsx"
      provides: "Sticky toolbar, debounced search, combinable filters, pagination (10/25/50), clear-filters action"
  key_links:
    - from: "search input state"
      to: "filtered assets list"
      via: "300ms debounced query + useMemo filter"
      pattern: "useEffect.*setTimeout|useMemo"
    - from: "category/status select state"
      to: "filtered assets list"
      via: "AND-combined predicates"
      pattern: "matchesCat && matchesStatus"
    - from: "filtered assets"
      to: "table rendering"
      via: "pagination slice"
      pattern: "slice\\(pageStart, pageEnd\\)"
---

<objective>
Implement list UX for Asset Registry with locked pagination, sticky filters, debounced search, and clear-filters behavior.

Purpose: Deliver ASSET-01/05/06 with full fidelity to Phase 6 locked decisions.
Output: Updated assets page with paginated/filterable/searchable table behavior.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-asset-registry-ui/06-CONTEXT.md
@.planning/phases/06-asset-registry-ui/06-UI-SPEC.md
@v0-ai-asset-management/app/dashboard/assets/page.tsx
@v0-ai-asset-management/lib/data.ts

<interfaces>
From `v0-ai-asset-management/lib/data.ts`:
```ts
export type AssetStatus = "registered" | "available" | "assigned" | "maintenance" | "retired"
export type Asset = { id: string; name: string; category: AssetCategory; serial: string; status: AssetStatus; ... }
```

From `v0-ai-asset-management/app/dashboard/assets/page.tsx`:
```ts
const [query, setQuery] = useState("")
const [category, setCategory] = useState<string>("all")
const [statusFilter, setStatusFilter] = useState<string>("all")
const filtered = useMemo(() => ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sticky toolbar + debounced search + combinable filters + clear action</name>
  <read_first>
    @.planning/phases/06-asset-registry-ui/06-CONTEXT.md
    @.planning/phases/06-asset-registry-ui/06-UI-SPEC.md
    @v0-ai-asset-management/app/dashboard/assets/page.tsx
  </read_first>
  <files>v0-ai-asset-management/app/dashboard/assets/page.tsx</files>
  <behavior>
    - Search input applies 300ms debounce before filter evaluation.
    - Query matches asset name OR asset ID OR serial.
    - Category and lifecycle filters combine with AND behavior.
    - "Clear filters" resets query/category/status and page to 1.
  </behavior>
  <action>
    Update toolbar behavior per D-01, D-08, D-09, D-10 and UI-SPEC: add `debouncedQuery` state with 300ms timer, keep filter bar sticky in scroll container, and add one-click clear button. Preserve base-ui null-guard pattern (`onValueChange={(v) => v && setX(v)}`) to avoid runtime regressions.
  </action>
  <verify>
    <automated>cd /Users/longtdang/Long/HCMUS/AI_Management_System/v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Search is debounced at 300ms; query checks name/id/serial; category+status filters combine; clear action resets all filters and pagination state.
  </acceptance_criteria>
  <done>Toolbar behavior matches locked decisions and compiles cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pagination controls and paged rendering (10/25/50)</name>
  <read_first>
    @.planning/phases/06-asset-registry-ui/06-CONTEXT.md
    @.planning/phases/06-asset-registry-ui/06-UI-SPEC.md
    @v0-ai-asset-management/app/dashboard/assets/page.tsx
  </read_first>
  <files>v0-ai-asset-management/app/dashboard/assets/page.tsx</files>
  <behavior>
    - Table shows only current page slice.
    - Page size selector supports exactly 10, 25, 50.
    - Pagination controls are rendered in table footer area.
    - Empty state appears when filtered result count is zero.
  </behavior>
  <action>
    Implement pagination state (`page`, `pageSize`) and footer controls per D-01 with exact page-size options 10/25/50. Ensure filter/search changes reset page to 1 to prevent blank pages. Keep lifecycle badges visible and preserve desktop-first column behavior from UI-SPEC.
  </action>
  <verify>
    <automated>cd /Users/longtdang/Long/HCMUS/AI_Management_System/v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Paginated table works with footer controls, page-size selector is 10/25/50, and lifecycle badges render correctly across pages.
  </acceptance_criteria>
  <done>ASSET-01/05/06 behaviors are fully implemented for list/filter/search/pagination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → client filter state | Untrusted query/filter values influence rendered dataset |
| role context → visible controls | Authorization context influences available UI actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | D (DoS-lite) | debounced search input | mitigate | Enforce 300ms debounce to reduce rapid re-render/filter churn. |
| T-06-02 | T (Tampering) | filter state transitions | mitigate | Reset page on filter/search change to avoid stale index state and misleading output. |
| T-06-03 | I (Information disclosure) | filtered table view | accept | UI-only mock data; no sensitive backend data boundary in this phase. |
</threat_model>

<verification>
Run `npm run build` in `v0-ai-asset-management` after each task and once after plan completion.
</verification>

<success_criteria>
- Paginated asset table works with 10/25/50 page sizes.
- Sticky toolbar supports debounced search over name/id/serial.
- Category + lifecycle filters combine correctly.
- One-click clear resets filters/search/page.
</success_criteria>

<output>
After completion, create `.planning/phases/06-asset-registry-ui/06-01-SUMMARY.md`
</output>
