---
phase: 06-asset-registry-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - v0-ai-asset-management/app/dashboard/assets/page.tsx
  - v0-ai-asset-management/components/asset-form-dialog.tsx
autonomous: true
requirements:
  - ASSET-02
  - ASSET-03
  - ASSET-04
must_haves:
  truths:
    - "Admin and Asset Manager can create a new asset using required fields."
    - "Admin and Asset Manager can click an asset row to edit prefilled data."
    - "Only Admin can retire assets, and retirement always requires confirmation."
    - "Retirement applies status retired, assignee null, location Disposal Storage."
  artifacts:
    - path: "v0-ai-asset-management/components/asset-form-dialog.tsx"
      provides: "Create/edit form with required fields and lifecycle status editable"
    - path: "v0-ai-asset-management/app/dashboard/assets/page.tsx"
      provides: "Role-gated create/edit/retire actions, row-click edit, confirmation dialog"
  key_links:
    - from: "Table row click"
      to: "AssetFormDialog initial asset"
      via: "openEdit(asset)"
      pattern: "onClick.*openEdit"
    - from: "Retire confirm CTA"
      to: "store retireAsset"
      via: "confirmRetire"
      pattern: "retireAsset\\("
    - from: "role checks"
      to: "retire visibility/execution"
      via: "admin-only boolean guard"
      pattern: "user\\?\\.role === \"Admin\""
---

<objective>
Implement form and destructive-action behavior for asset create/edit/retire with strict role gating.

Purpose: Deliver ASSET-02/03/04 without reducing locked permission and confirmation decisions.
Output: Updated assets page and asset form dialog with role-accurate interactions.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-asset-registry-ui/06-CONTEXT.md
@.planning/phases/06-asset-registry-ui/06-UI-SPEC.md
@v0-ai-asset-management/app/dashboard/assets/page.tsx
@v0-ai-asset-management/components/asset-form-dialog.tsx
@v0-ai-asset-management/lib/store.tsx

<interfaces>
From `v0-ai-asset-management/lib/store.tsx`:
```ts
addAsset: (asset: Asset) => void
updateAsset: (id: string, patch: Partial<Asset>) => void
retireAsset: (id: string) => void // sets status retired, assignee null, location Disposal Storage
```

From `v0-ai-asset-management/components/asset-form-dialog.tsx`:
```ts
type Props = { open: boolean; onOpenChange: (v: boolean) => void; initial?: Asset | null; onSave: (asset: Asset) => void }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce create/edit permissions and row-click edit interaction</name>
  <read_first>
    @.planning/phases/06-asset-registry-ui/06-CONTEXT.md
    @.planning/phases/06-asset-registry-ui/06-UI-SPEC.md
    @v0-ai-asset-management/app/dashboard/assets/page.tsx
    @v0-ai-asset-management/components/asset-form-dialog.tsx
  </read_first>
  <files>v0-ai-asset-management/app/dashboard/assets/page.tsx, v0-ai-asset-management/components/asset-form-dialog.tsx</files>
  <behavior>
    - Admin/Asset Manager can open create dialog.
    - Clicking a non-action cell in a table row opens prefilled edit dialog.
    - Required fields are enforced in form submit path.
    - Asset ID auto-generates on create and lifecycle status remains editable on create/edit.
  </behavior>
  <action>
    Implement D-01, D-02, D-03, D-04 by wiring row click to `openEdit`, keeping explicit action controls separate (do not trigger edit from destructive controls), and aligning copy with UI-SPEC primary CTA text ("Create Asset"). Keep required fields and auto-ID generation in `AssetFormDialog`; do not remove lifecycle status editability.
  </action>
  <verify>
    <automated>cd /Users/longtdang/Long/HCMUS/AI_Management_System/v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Create/edit are accessible only to Admin/Asset Manager, row click opens prefilled edit, and form requirements + ID generation + lifecycle-status editability are intact.
  </acceptance_criteria>
  <done>ASSET-02 and ASSET-03 are satisfied with locked UX behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce Admin-only retire with mandatory confirmation and secure guard</name>
  <read_first>
    @.planning/phases/06-asset-registry-ui/06-CONTEXT.md
    @.planning/phases/06-asset-registry-ui/06-UI-SPEC.md
    @v0-ai-asset-management/app/dashboard/assets/page.tsx
    @v0-ai-asset-management/lib/store.tsx
  </read_first>
  <files>v0-ai-asset-management/app/dashboard/assets/page.tsx</files>
  <behavior>
    - Retire action is visible only for Admin.
    - Confirmation dialog always appears before retirement mutation.
    - Confirmed retire applies retired/null assignee/Disposal Storage state effect.
    - Non-admin UI paths cannot execute retire handler.
  </behavior>
  <action>
    Implement D-05, D-06, D-07 with explicit `canRetire = user?.role === "Admin"` and a second guard inside `confirmRetire` before calling `retireAsset` (defense-in-depth for UI tampering). Keep destructive copy exactly aligned to UI-SPEC and preserve success toast behavior for create/edit/retire.
  </action>
  <verify>
    <automated>cd /Users/longtdang/Long/HCMUS/AI_Management_System/v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Only Admin sees and can execute retire, confirmation is mandatory, and post-retire state is status=retired + assignee cleared + location set to Disposal Storage.
  </acceptance_criteria>
  <done>ASSET-04 is fully implemented with locked role and confirmation constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role context → destructive control | UI authorization state gates access to retirement action |
| confirmation dialog → mutation call | User confirmation gates destructive store mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | E (Elevation of privilege) | retire action visibility/execution | mitigate | Use Admin-only visibility and in-handler Admin guard before `retireAsset`. |
| T-06-05 | T (Tampering) | destructive action trigger | mitigate | Force confirm dialog path; do not expose direct retire call from row click. |
| T-06-06 | R (Repudiation-lite) | destructive UX action | mitigate | Keep explicit success toast for retire action to provide user-visible operation feedback trace in UI. |
</threat_model>

<verification>
Run `npm run build` in `v0-ai-asset-management` after each task and once after plan completion.
</verification>

<success_criteria>
- Admin/Asset Manager can create/edit assets through required-field form.
- Row click opens prefilled edit dialog.
- Admin-only retire action is confirmation-gated and applies locked retirement effects.
</success_criteria>

<output>
After completion, create `.planning/phases/06-asset-registry-ui/06-02-SUMMARY.md`
</output>
