---
phase: 05-foundation-layout-dashboard
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - v0-ai-asset-management/lib/dashboard-kpis.ts
  - v0-ai-asset-management/lib/dashboard-kpis.test.ts
  - v0-ai-asset-management/app/dashboard/page.tsx
  - v0-ai-asset-management/app/dashboard/page.test.tsx
autonomous: true
requirements: [DASH-01]
must_haves:
  truths:
    - "Dashboard KPI card set shows exactly four required cards: Total Assets, Active Assignments, Assets in Maintenance, Warranty Expiring Soon."
    - "Warranty KPI value is derived from assets expiring within <= 3 months, not hardcoded."
    - "Warranty alert panel remains visible in alerts section."
  artifacts:
    - path: "v0-ai-asset-management/lib/dashboard-kpis.ts"
      provides: "Typed KPI contract and label set for DASH-01"
    - path: "v0-ai-asset-management/app/dashboard/page.tsx"
      provides: "Dashboard KPI rendering wired to contract-driven values"
    - path: "v0-ai-asset-management/app/dashboard/page.test.tsx"
      provides: "UI regression test for KPI-card contract"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "lib/dashboard-kpis.ts"
      via: "KPI card config import/use"
      pattern: "dashboard-kpis"
    - from: "app/dashboard/page.test.tsx"
      to: "app/dashboard/page.tsx"
      via: "render assertions for KPI labels/count"
      pattern: "Warranty Expiring Soon"
---

<objective>
Close the remaining functional Phase 5 gap by enforcing the DASH-01 KPI card contract in dashboard UI.

Purpose: Remove KPI-card mismatch flagged by milestone audit.
Output: Contract-driven KPI implementation and regression tests that prevent relapses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-foundation-layout-dashboard/05-CONTEXT.md
@.planning/phases/05-foundation-layout-dashboard/05-RESEARCH.md
@v0-ai-asset-management/app/dashboard/page.tsx
@v0-ai-asset-management/vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock DASH-01 KPI contract in typed helper + regression tests</name>
  <files>v0-ai-asset-management/lib/dashboard-kpis.ts, v0-ai-asset-management/lib/dashboard-kpis.test.ts</files>
  <behavior>
    - Test 1: KPI contract exposes exactly 4 KPI definitions per D-01.
    - Test 2: KPI labels are exactly "Total Assets", "Active Assignments", "Assets in Maintenance", "Warranty Expiring Soon" per D-01.
    - Test 3: Contract explicitly excludes "Original Value" from KPI-card set per D-01.
  </behavior>
  <action>Create a typed KPI contract helper and tests first (RED->GREEN) per D-05. Keep this helper dashboard-local and focused only on KPI-card contract; do not add unrelated analytics logic (per D-04).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm run test -- lib/dashboard-kpis.test.ts</automated>
  </verify>
  <done>Typed KPI contract exists, and contract tests pass enforcing DASH-01 label/count exactness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire dashboard KPI cards to contract and replace Original Value with Warranty Expiring Soon</name>
  <files>v0-ai-asset-management/app/dashboard/page.tsx, v0-ai-asset-management/app/dashboard/page.test.tsx</files>
  <behavior>
    - Test 1: Dashboard renders the four required KPI card labels per D-01.
    - Test 2: Dashboard does not render "Original Value" as a KPI card label per D-01.
    - Test 3: Warranty alert panel still renders in alerts section per D-02.
  </behavior>
  <action>Refactor KPI rendering in `app/dashboard/page.tsx` to consume the new helper and drive values from existing computed stats (`active.length`, `activeAssignments`, `maintenance`, `warrantySoon.length`) per D-01. Keep existing warranty alert panel behavior intact per D-02. Add/refresh dashboard page tests to assert label contract and alert-panel continuity per D-05. Avoid any Phase 7/11/12 changes (D-04).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm run test -- app/dashboard/page.test.tsx lib/dashboard-kpis.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard KPI cards satisfy DASH-01 exactly, tests pass, and TypeScript remains clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store data -> KPI display | Incorrect derivation can misrepresent dashboard contract values |
| KPI contract helper -> UI render | Contract drift can silently reintroduce DASH-01 mismatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | `app/dashboard/page.tsx` KPI labels | mitigate | Enforce exact label/count assertions in `app/dashboard/page.test.tsx` (D-05). |
| T-05-02-02 | R | `DASH-01` closure evidence | mitigate | Require passing automated test commands and `tsc --noEmit` before sign-off. |
| T-05-02-03 | I | Alert-panel behavior | mitigate | Add explicit assertion that warranty alert panel remains present (D-02). |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npm run test -- lib/dashboard-kpis.test.ts`
- `cd v0-ai-asset-management && npm run test -- app/dashboard/page.test.tsx lib/dashboard-kpis.test.ts`
- `cd v0-ai-asset-management && npx tsc --noEmit`
</verification>

<success_criteria>
- `DASH-01` KPI-card mismatch is eliminated with contract-backed tests.
- Dashboard retains warranty alert panel behavior.
- No files outside scoped Phase 5 closure targets are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-layout-dashboard/05-02-SUMMARY.md`
</output>
