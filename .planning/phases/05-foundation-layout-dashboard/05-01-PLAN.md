---
phase: 5
plan: 1
name: Foundation, Layout & Dashboard
status: complete
gap_closure: false
---

# Phase 5 Plan: Foundation, Layout & Dashboard

## Objective

Rewrite the entire v0-ai-asset-management UI in English with mock data aligned to the v1.0 architecture. Establish the 4-role RBAC foundation, v1.0 lifecycle states, and all 9 module navigation items so every subsequent phase can build on a solid, type-safe base.

## Tasks

1. Rewrite `lib/data.ts` — English types (`AssetStatus`, `AssignmentRecord`, `MaintenanceRecord`, `WarrantyRecord`, `UserRole`), USD prices, `formatCurrency`
2. Rewrite `lib/store.tsx` — 4-role `DEMO_USERS`, new store API (`createAssignment`, `initiateReturn`, `approveAssignment`, `rejectAssignment`, `retireAsset`, `updateMaintenanceStatus`)
3. Update `components/status-badge.tsx` — English status keys + LABELS display map
4. Rewrite `components/sidebar.tsx` — 9 nav items, 4-role visibility filter
5. Update `components/topbar.tsx` — 4-role badge variant
6. Rewrite `app/page.tsx` — English login, 2×2 role picker
7. Update `app/dashboard/layout.tsx` — English redirect message
8. Rewrite `app/dashboard/page.tsx` — English dashboard, `assignmentRecords`, `formatCurrency`
9. Rewrite `components/asset-form-dialog.tsx` — English labels/values
10. Rewrite `app/dashboard/assets/page.tsx` — English, `retireAsset`, status filter
11. Rewrite `app/dashboard/borrow/page.tsx` → Assignments page using `assignmentRecords`
12. Rewrite `lib/assistant.ts` — English NL→query engine, trace/confidence fields
13. Update `app/dashboard/assistant/page.tsx` — English UI, `formatCurrency`
14. Rewrite `app/dashboard/ocr/page.tsx` — English, confidence-band UX, `formatCurrency`
15. Rewrite `app/dashboard/reports/page.tsx` — English labels, `formatCurrency`

## Verification

- TypeScript: 0 errors (`tsc --noEmit`)
- All UI text in English (no Vietnamese)
- All 4 roles navigate to correct sections
- Dashboard renders without runtime errors
