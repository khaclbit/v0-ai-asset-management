---
phase: 08-maintenance-warranty-ui
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - v0-ai-asset-management/app/dashboard/maintenance/page.tsx
autonomous: true
requirements: [MAINT-03, MAINT-04]
requirements_addressed: [MAINT-03, MAINT-04]
must_haves:
  truths:
    - "User can view warranty tracker records with status badges and urgency-first default ordering."
    - "User can search/filter tracker rows and clear filters in one action."
    - "User sees <=30 day expiry warnings in a top alert and highlighted rows, and can jump to the related record."
  artifacts:
    - path: "v0-ai-asset-management/app/dashboard/maintenance/page.tsx"
      provides: "Warranty tracker, warning summary, row highlighting, warning-item jump/filter behavior"
  key_links:
    - from: "warning summary list item"
      to: "warranty tracker controls"
      via: "click handler setting search/filter/focus state"
      pattern: "onClick.*set"
    - from: "warranty row rendering"
      to: "endDate calculation"
      via: "days-delta helper"
      pattern: "new Date\(record.endDate\)"
---

<objective>
Complete warranty tracking and warning UX for Phase 8.

Purpose: Satisfy MAINT-03 and MAINT-04 while honoring urgency, filtering, and warning-jump decisions.
Output: Warranty tracker + 30-day warning system integrated into maintenance route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-maintenance-warranty-ui/08-CONTEXT.md
@.planning/phases/08-maintenance-warranty-ui/08-RESEARCH.md
@.planning/phases/08-maintenance-warranty-ui/08-UI-SPEC.md
@v0-ai-asset-management/app/dashboard/maintenance/page.tsx
@v0-ai-asset-management/lib/maintenance-warranty.ts
@v0-ai-asset-management/components/status-badge.tsx

<interfaces>
From lib/data.ts:
```typescript
export type WarrantyStatus = "active" | "expiring_soon" | "expired" | "void"
export type WarrantyRecord = {
  id: string
  assetId: string
  assetName: string
  provider: string
  endDate: string
  status: WarrantyStatus
  coverageNotes: string
}
```

From StatusBadge:
```typescript
export function StatusBadge({ status }: { status: string })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build warranty tracker list with ordering and controls</name>
  <read_first>@v0-ai-asset-management/app/dashboard/maintenance/page.tsx, @v0-ai-asset-management/lib/maintenance-warranty.ts, @.planning/phases/08-maintenance-warranty-ui/08-CONTEXT.md</read_first>
  <files>v0-ai-asset-management/app/dashboard/maintenance/page.tsx</files>
  <behavior>
    - Test 1: default tracker order is expiring_soon, expired, active, void.
    - Test 2: search (asset/provider) and status filter can combine, and clear-filters resets both.
    - Test 3: each row shows end date plus relative timing badge (`days left` or `Expired X days ago`).
  </behavior>
  <action>Extend the maintenance route with a warranty tracker section that renders MAINT-03 statuses using `StatusBadge`, ordered urgency-first (D-09). Add search input (asset/provider), status filter select, and clear-filters button (D-10). Show end date and relative-time badge text per row (D-11). Keep `void` items in the same list with muted row styling and explicit reason text derived from coverage notes (D-12).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>Warranty table behavior matches locked ordering/filtering/relative-time decisions and remains on the maintenance route.</acceptance_criteria>
  <done>MAINT-03 row rendering, controls, and badge states are fully implemented without placeholder data paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add top warning summary and clickable jump-to-row behavior</name>
  <read_first>@v0-ai-asset-management/app/dashboard/maintenance/page.tsx, @.planning/phases/08-maintenance-warranty-ui/08-UI-SPEC.md</read_first>
  <files>v0-ai-asset-management/app/dashboard/maintenance/page.tsx</files>
  <behavior>
    - Test 1: assets expiring in 0-7 days are tagged critical; 8-30 days are warning.
    - Test 2: summary warning panel appears at top and corresponding rows are highlighted.
    - Test 3: clicking a warning item applies tracker focus/filter to its asset row.
  </behavior>
  <action>Compute expiry urgency from `endDate` for all warranties within <=30 days (not status-only) and render a top notification-style summary alert plus row-level highlighting (D-13, MAINT-04). Apply tier styles: 0-7 days critical (destructive), 8-30 warning (muted warning tone) (D-14). Keep warning area visible for all roles already allowed on this route (D-15). Implement click on summary item to jump/filter tracker to the related asset record and visually focus it (D-16).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm run build</automated>
  </verify>
  <acceptance_criteria>Warning summary and row highlights stay in sync from date-based calculations, and warning clicks reliably move user context to target rows.</acceptance_criteria>
  <done>MAINT-04 is fully implemented with both summary and row-level warning surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Final role visibility and regression pass for maintenance/warranty page</name>
  <read_first>@v0-ai-asset-management/components/sidebar.tsx, @v0-ai-asset-management/app/dashboard/layout.tsx, @v0-ai-asset-management/app/dashboard/maintenance/page.tsx</read_first>
  <files>v0-ai-asset-management/app/dashboard/maintenance/page.tsx</files>
  <action>Run a focused regression pass in code: confirm non-Asset-Manager roles on this route only receive read-only maintenance controls while still seeing warranty warnings (D-15), ensure blocked/error copy and iconography follow UI-SPEC discretion constraints, and remove any dead state/handlers introduced in Plan 01–02 integration.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>No role leakage for mutation actions; all maintenance/warranty UI sections render for authorized roles with no type/build regressions.</acceptance_criteria>
  <done>Phase 8 page is coherent end-to-end with clean build and no unresolved TODO placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Warranty date data -> urgency rendering | Incorrect parsing can hide urgent warnings |
| Warning panel click -> tracker filter state | Untrusted UI interactions could desync focus/filter state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | T (Tampering) | Warranty urgency computation | High | mitigate (blocking) | Derive urgency from `endDate` delta on render; do not trust stored `status` for <=30-day warning logic. |
| T-08-06 | I (Information Disclosure) | Warranty coverage notes display | Medium | mitigate | Show only record-local reason text already present in mock dataset; avoid exposing unrelated entity fields. |
| T-08-07 | D (Denial of Service) | Warning click/filter loop | Low | accept | In-memory dataset is bounded; simple state updates are sufficient for v1.1 scope. |
| T-08-08 | S (Spoofing) | Warning severity badge style | Medium | mitigate | Bind critical/warning style directly to computed day thresholds so UI cannot spoof severity via stale status label. |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npx tsc --noEmit`
- `cd v0-ai-asset-management && npm run build`
- Confirm warning summary click narrows/focuses tracker and can be reset with Clear Filters.
</verification>

<success_criteria>
- MAINT-03: Warranty tracker list with active/expiring_soon/expired/void badges is visible and filterable.
- MAINT-04: <=30 day warnings appear in top summary and highlighted rows with 0-7/8-30 urgency tiers.
- High-severity threat T-08-05 is mitigated before completion.
</success_criteria>

<output>
After completion, create `.planning/phases/08-maintenance-warranty-ui/08-02-SUMMARY.md`
</output>
