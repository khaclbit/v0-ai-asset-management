---
phase: 08-maintenance-warranty-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - v0-ai-asset-management/lib/maintenance-warranty.ts
  - v0-ai-asset-management/lib/store.tsx
  - v0-ai-asset-management/app/dashboard/maintenance/page.tsx
autonomous: true
requirements: [MAINT-01, MAINT-02]
requirements_addressed: [MAINT-01, MAINT-02]
must_haves:
  truths:
    - "User can view maintenance records grouped by status with proper badges and blocked-row emphasis."
    - "Asset Manager can update maintenance status inline with immediate badge refresh and success toast."
    - "Invalid status transitions and blocked-without-note attempts are rejected."
  artifacts:
    - path: "v0-ai-asset-management/lib/maintenance-warranty.ts"
      provides: "Transition guards and status metadata utilities for maintenance/warranty UI"
    - path: "v0-ai-asset-management/lib/store.tsx"
      provides: "Guarded maintenance status mutation with optional notes"
    - path: "v0-ai-asset-management/app/dashboard/maintenance/page.tsx"
      provides: "Maintenance grouped sections and inline status update UI"
  key_links:
    - from: "app/dashboard/maintenance/page.tsx"
      to: "lib/maintenance-warranty.ts"
      via: "transition and grouping helpers"
      pattern: "canTransitionMaintenance|MAINTENANCE_GROUP_ORDER"
    - from: "app/dashboard/maintenance/page.tsx"
      to: "lib/store.tsx"
      via: "updateMaintenanceStatus mutation"
      pattern: "updateMaintenanceStatus\("
---

<objective>
Build the maintenance management surface for Phase 8.

Purpose: Satisfy MAINT-01 and MAINT-02 with decision-locked grouping UX and safe inline updates.
Output: New maintenance page + guarded store mutation + shared transition helper module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-maintenance-warranty-ui/08-CONTEXT.md
@.planning/phases/08-maintenance-warranty-ui/08-RESEARCH.md
@.planning/phases/08-maintenance-warranty-ui/08-UI-SPEC.md
@v0-ai-asset-management/lib/data.ts
@v0-ai-asset-management/lib/store.tsx
@v0-ai-asset-management/components/status-badge.tsx

<interfaces>
From lib/data.ts:
```typescript
export type MaintenanceStatus = "scheduled" | "in_progress" | "completed" | "blocked"
export type MaintenanceRecord = {
  id: string
  assetId: string
  assetName: string
  type: "scheduled" | "risk_based" | "warranty"
  priority: "high" | "medium" | "low"
  status: MaintenanceStatus
  scheduledDate: string
  completedDate: string | null
  notes: string
}
```

From lib/store.tsx (must be preserved/extended):
```typescript
maintenanceRecords: MaintenanceRecord[]
updateMaintenanceStatus: (id: string, status: MaintenanceStatus) => void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared maintenance/warranty contract helpers</name>
  <read_first>@v0-ai-asset-management/lib/data.ts, @.planning/phases/08-maintenance-warranty-ui/08-CONTEXT.md, @.planning/phases/08-maintenance-warranty-ui/08-UI-SPEC.md</read_first>
  <files>v0-ai-asset-management/lib/maintenance-warranty.ts</files>
  <behavior>
    - Test 1: `canTransitionMaintenance("scheduled", "in_progress")` returns true.
    - Test 2: `canTransitionMaintenance("scheduled", "completed")` returns false.
    - Test 3: blocked-note validation helper returns false when status=blocked and note empty.
  </behavior>
  <action>Create `lib/maintenance-warranty.ts` exporting decision-locked constants and guards: maintenance group order (D-01), per-group date sorting helper (D-03), blocked-state marker helper (D-04), transition validator enforcing only `scheduled->in_progress->completed` and `blocked->in_progress` (D-06), and blocked-note requirement validator (D-07). Keep pure functions only (no React state in this file) so both store and UI use the same rules.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>Helper module exports typed constants/functions for grouping, sorting, transition checks, and blocked-note validation; no inline magic strings duplicated in page/store.</acceptance_criteria>
  <done>Contracts exist and compile; downstream tasks import from this module instead of re-declaring transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden store mutation for maintenance status changes</name>
  <read_first>@v0-ai-asset-management/lib/store.tsx, @v0-ai-asset-management/lib/maintenance-warranty.ts</read_first>
  <files>v0-ai-asset-management/lib/store.tsx</files>
  <behavior>
    - Test 1: valid transition updates record status and keeps existing notes unless override passed.
    - Test 2: invalid transition is ignored with no state mutation.
    - Test 3: setting blocked without note is rejected.
  </behavior>
  <action>Update StoreContext signature and `updateMaintenanceStatus` implementation to accept `{ status, notes? }`. Enforce role-agnostic data integrity with transition validator + blocked-note requirement from Task 1 (D-06, D-07). Preserve immediate update semantics (D-08) by returning updated record in the same mutation pass and setting `completedDate` only when status becomes `completed`.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>Store rejects invalid transitions and blocked-without-note attempts while permitting valid updates used by UI controls.</acceptance_criteria>
  <done>`useStore().updateMaintenanceStatus` is safely typed and guarded; existing app compiles with updated call sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement grouped maintenance UI with inline manager updates</name>
  <read_first>@v0-ai-asset-management/app/dashboard/borrow/page.tsx, @v0-ai-asset-management/components/status-badge.tsx, @v0-ai-asset-management/lib/store.tsx, @v0-ai-asset-management/lib/maintenance-warranty.ts</read_first>
  <files>v0-ai-asset-management/app/dashboard/maintenance/page.tsx</files>
  <behavior>
    - Test 1: page renders four maintenance groups with rows sorted by earliest scheduled date.
    - Test 2: Asset Manager can change row status via inline select and sees immediate badge update + success toast.
    - Test 3: attempting blocked without note shows phase-approved error copy and does not mutate row.
  </behavior>
  <action>Create `/dashboard/maintenance` page implementing maintenance schedule sections grouped by status (D-01) with row fields: asset, maintenance type, priority, scheduled date, status, note snippet (D-02). Apply per-group ascending date sort (D-03). Render blocked rows with red tint, blocked badge, and visible blocking note (D-04). Add inline Asset Manager status selector (D-05) plus optional detail modal for notes; enforce transition + note rules via helper/store (D-06, D-07). On valid updates, call store mutation and show success toast with immediate badge refresh (D-08). Non-managers must see read-only statuses (no update controls).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm run build</automated>
  </verify>
  <acceptance_criteria>Maintenance list is fully interactive for Asset Manager and read-only for other permitted roles, with all locked maintenance decisions visible in UI behavior.</acceptance_criteria>
  <done>MAINT-01 and MAINT-02 are satisfiable from the route without placeholder behavior; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI -> Store mutation | Untrusted user actions attempt status updates |
| Inline note input -> maintenance records | Free-text can bypass required blocked-note rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | E (Elevation of Privilege) | `app/dashboard/maintenance/page.tsx` status actions | High | mitigate (blocking) | Gate mutating controls to Asset Manager role and enforce guard in handler before calling mutation. |
| T-08-02 | T (Tampering) | `lib/store.tsx` `updateMaintenanceStatus` | High | mitigate (blocking) | Validate legal transitions and blocked-note requirement in store using shared helper before state update. |
| T-08-03 | R (Repudiation) | status update feedback | Medium | mitigate | Emit deterministic success/error toast messages so unauthorized/invalid attempts are user-visible and not silent. |
| T-08-04 | D (Denial of Service) | grouped render loops | Low | accept | Mock-data list size is small in v1.1; no pagination required in this phase. |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npx tsc --noEmit`
- `cd v0-ai-asset-management && npm run build`
- Confirm `/dashboard/maintenance` loads and grouped sections render without runtime errors.
</verification>

<success_criteria>
- MAINT-01: Maintenance schedule list exists with all four state badges.
- MAINT-02: Asset Manager can update status inline with immediate UI feedback and guarded transitions.
- High-severity threats T-08-01 and T-08-02 are mitigated in code paths before plan completion.
</success_criteria>

<output>
After completion, create `.planning/phases/08-maintenance-warranty-ui/08-01-SUMMARY.md`
</output>
