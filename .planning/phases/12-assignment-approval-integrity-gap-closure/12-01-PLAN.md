---
phase: 12-assignment-approval-integrity-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - v0-ai-asset-management/lib/assignment-approval.ts
  - v0-ai-asset-management/lib/assignment-approval.test.ts
  - v0-ai-asset-management/lib/store.tsx
  - v0-ai-asset-management/app/dashboard/borrow/page.tsx
  - v0-ai-asset-management/app/dashboard/borrow/page.test.tsx
autonomous: true
requirements: [ASGN-02]
must_haves:
  truths:
    - "When approval conflicts, the request remains requested and the related asset does not change assignee/status."
    - "When approval succeeds, assignment moves to active and related asset is assigned to the same assignee."
    - "UI gives explicit conflict feedback instead of success toast on failed approval."
  artifacts:
    - path: "v0-ai-asset-management/lib/assignment-approval.ts"
      provides: "Typed approval result contract and guarded transition logic"
    - path: "v0-ai-asset-management/lib/store.tsx"
      provides: "Single success-only approval transition wiring assignment+asset updates"
    - path: "v0-ai-asset-management/app/dashboard/borrow/page.tsx"
      provides: "Result-aware approve handler with success/conflict toasts"
  key_links:
    - from: "lib/store.tsx approveAssignment"
      to: "lib/assignment-approval.ts"
      via: "typed approval decision result"
      pattern: "ok: true|false"
    - from: "app/dashboard/borrow/page.tsx handleApprove"
      to: "store approveAssignment result"
      via: "toast branching by result.reason"
      pattern: "if \\(!result.ok\\)"
---

<objective>
Close the assignment approval integrity gap by enforcing one guarded approval transition and explicit conflict feedback.

Purpose: Prevent conflict-path side-effect desynchronization while preserving successful approval behavior.
Output: Typed approval contract, atomic store transition, UI feedback branching, and regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-assignment-approval-integrity-gap-closure/12-CONTEXT.md
@.planning/phases/12-assignment-approval-integrity-gap-closure/12-RESEARCH.md
@v0-ai-asset-management/lib/store.tsx
@v0-ai-asset-management/app/dashboard/borrow/page.tsx

<interfaces>
From `v0-ai-asset-management/lib/store.tsx`:
```ts
approveAssignment: (id: string) => void
```

From `v0-ai-asset-management/lib/data`:
```ts
type AssignmentStatus = "requested" | "active" | "overdue" | "closed" | "rejected"
type AssetStatus = "registered" | "available" | "assigned" | "maintenance" | "retired"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define approval integrity contract and conflict/success transition tests</name>
  <files>v0-ai-asset-management/lib/assignment-approval.ts, v0-ai-asset-management/lib/assignment-approval.test.ts</files>
  <behavior>
    - Test 1: Conflict case returns `ok:false` and leaves assignment in `requested` with no asset patch (D-01, D-03).
    - Test 2: Success case returns `ok:true` with assignment -> `active` and asset sync patch for status/assignee (D-04, D-05).
    - Test 3: Non-requested/non-existent requests return explicit failure reason for UI handling (D-02).
  </behavior>
  <action>Create a pure typed helper that computes approval outcome (`ok` + reason + transition payload) using a single guarded transition model (per D-04). Do not mutate in helper; return explicit instructions the store applies only when `ok:true` (D-01, D-03, D-05).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- lib/assignment-approval.test.ts</automated>
  </verify>
  <done>Approval decisions are fully represented by a typed contract with tests locking conflict and success behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor store approveAssignment to success-only atomic side effects</name>
  <files>v0-ai-asset-management/lib/store.tsx</files>
  <behavior>
    - Test 1: Conflict approval keeps assignment requested and leaves asset unchanged (D-01, D-03).
    - Test 2: Successful approval updates assignment and matching asset together (D-04, D-05).
  </behavior>
  <action>Update `approveAssignment` to return typed result contract and apply assignment+asset mutations only in success branch from the guard helper (per D-04). Remove the current two-step pattern that can run asset sync after no-op assignment update. Keep existing successful semantics unchanged (D-05).</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- lib/assignment-approval.test.ts</automated>
  </verify>
  <done>Store enforces one guarded approval path; conflict path has zero asset side effects; function returns structured result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add result-aware approve UX feedback and page-level regression tests</name>
  <files>v0-ai-asset-management/app/dashboard/borrow/page.tsx, v0-ai-asset-management/app/dashboard/borrow/page.test.tsx</files>
  <behavior>
    - Test 1: On `approveAssignment` failure, conflict error toast is shown and success toast is not shown (D-02).
    - Test 2: On `approveAssignment` success, existing success toast remains (D-05).
  </behavior>
  <action>Change `handleApprove` to consume the new result contract and show explicit conflict failure toast on `ok:false` (per D-02). Preserve existing success toast copy/flow for `ok:true` (per D-05). Keep scope limited to approval integrity handling only.</action>
  <verify>
    <automated>cd v0-ai-asset-management && npm test -- app/dashboard/borrow/page.test.tsx && npm run build</automated>
  </verify>
  <done>Approve UI distinguishes success vs conflict paths and regression tests prevent silent desync behavior from returning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approve action UI -> store transition | User-triggered approval crosses into state mutation logic |
| Assignment state -> asset state sync | Integrity-sensitive linkage where desync can corrupt workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | `lib/store.tsx` approval flow | mitigate | Enforce single guarded transition and apply side effects only when approval outcome is `ok:true` (D-04). |
| T-12-02 | R | `app/dashboard/borrow/page.tsx` feedback | mitigate | Return explicit failure reason and show conflict error toast for failed approvals (D-02). |
| T-12-03 | D | assignment/asset state linkage | mitigate | Add regression tests for both conflict no-op and success sync branches to block future desync regressions (D-01, D-03, D-05). |
</threat_model>

<verification>
- `cd v0-ai-asset-management && npm test -- lib/assignment-approval.test.ts app/dashboard/borrow/page.test.tsx`
- `cd v0-ai-asset-management && npm run build`
</verification>

<success_criteria>
- ASGN-02 gap closure: Asset Manager approve/reject flow remains functional.
- Conflict approvals never mutate asset assignee/status while assignment remains requested.
- Successful approvals still transition assignment to active and sync asset assignment data.
</success_criteria>

<output>
After completion, create `.planning/phases/12-assignment-approval-integrity-gap-closure/12-01-SUMMARY.md`
</output>
